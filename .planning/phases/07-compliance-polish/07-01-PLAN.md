---
phase: 07-compliance-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/002_compliance_polish.py
  - app/models/job_run.py
  - app/models/sync_metadata.py
  - app/models/job_role_compliance.py
  - app/services/job_manager_service.py
  - app/container.py
autonomous: true
requirements: [COMP-01, COMP-04, COMP-05]

must_haves:
  truths:
    - "JobRun model persists background job state in PostgreSQL"
    - "SyncMetadata model tracks warehouse sync timestamps and errors"
    - "ComplianceCheckRun has checked_count for incremental progress"
    - "JobManagerService runs background jobs with mutex and app context"
    - "JobManagerService is registered in DI container"
  artifacts:
    - path: "alembic/versions/002_compliance_polish.py"
      provides: "Schema migration for checked_count, sync_metadata, job_runs"
      contains: "op.add_column"
    - path: "app/models/job_run.py"
      provides: "JobRun model for job manager state"
      exports: ["JobRun"]
    - path: "app/models/sync_metadata.py"
      provides: "SyncMetadata model for warehouse sync tracking"
      exports: ["SyncMetadata"]
    - path: "app/services/job_manager_service.py"
      provides: "Background job execution with mutex and progress"
      exports: ["JobManagerService", "ConflictError"]
  key_links:
    - from: "app/services/job_manager_service.py"
      to: "app/models/job_run.py"
      via: "JobRun model for state persistence"
      pattern: "JobRun\\.query"
    - from: "app/container.py"
      to: "app/services/job_manager_service.py"
      via: "DI registration"
      pattern: "job_manager"
---

<objective>
Create the foundational data layer and job management infrastructure for Phase 7.

Purpose: All subsequent plans (job endpoints, progress polling, sync status) depend on these models and the job manager service existing and being functional.
Output: Alembic migration, 2 new models, modified ComplianceCheckRun, JobManagerService registered in DI container.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-compliance-polish/07-RESEARCH.md
@.planning/phases/07-compliance-polish/07-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration and models</name>
  <files>alembic/versions/002_compliance_polish.py, app/models/job_run.py, app/models/sync_metadata.py, app/models/job_role_compliance.py</files>
  <read_first>
    - alembic/versions/001_baseline_from_live_schema.py (migration pattern: revision chain, upgrade/downgrade structure)
    - app/models/job_role_compliance.py (ComplianceCheckRun model at line 304 — add checked_count column)
    - app/models/base.py (BaseModel, TimestampMixin imports and patterns)
  </read_first>
  <action>
    1. Create `app/models/job_run.py` with class `JobRun(BaseModel, TimestampMixin)`:
       - __tablename__ = "job_runs"
       - run_id: String(100), unique, nullable=False, index=True
       - job_name: String(100), nullable=False, index=True
       - status: String(50), default="running", index=True (values: running, completed, failed)
       - started_at: DateTime(timezone=True), default=utcnow
       - completed_at: DateTime(timezone=True), nullable
       - error: Text, nullable
       - triggered_by: String(255), default="system"

    2. Create `app/models/sync_metadata.py` with class `SyncMetadata(BaseModel, TimestampMixin)`:
       - __tablename__ = "sync_metadata"
       - sync_type: String(50), unique=True, nullable=False, index=True
       - last_success_at: DateTime(timezone=True), nullable
       - last_error_at: DateTime(timezone=True), nullable
       - last_error_message: Text, nullable
       - last_error_category: String(100), nullable
       - total_records_synced: Integer, default=0
       - duration_seconds: Integer, nullable

    3. Modify `app/models/job_role_compliance.py` — add to ComplianceCheckRun class (after line 321 `error_count` column):
       - checked_count = db.Column(db.Integer, default=0)

    4. Create Alembic migration `alembic/versions/002_compliance_polish.py`:
       - revision = generated hash (use alembic revision command or manual)
       - down_revision = revision ID from 001_baseline_from_live_schema.py
       - upgrade: op.add_column("compliance_check_runs", sa.Column("checked_count", sa.Integer(), server_default="0"))
       - upgrade: op.create_table("sync_metadata", ...) with all columns from step 2
       - upgrade: op.create_table("job_runs", ...) with all columns from step 1
       - downgrade: reverse all operations

    Per D-04 (checked_count field for incremental progress), D-08 (sync_metadata for local sync record), D-01 (job state in PostgreSQL).
  </action>
  <verify>
    <automated>cd /Users/jslitzker/Repos/Who-Dis && python -c "from app.models.job_run import JobRun; from app.models.sync_metadata import SyncMetadata; from app.models.job_role_compliance import ComplianceCheckRun; assert hasattr(ComplianceCheckRun, 'checked_count'); print('Models OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/models/job_run.py` exports `JobRun` class with `__tablename__ = "job_runs"`
    - `app/models/sync_metadata.py` exports `SyncMetadata` class with `__tablename__ = "sync_metadata"`
    - `ComplianceCheckRun` has `checked_count = db.Column(db.Integer, default=0)` attribute
    - `alembic/versions/002_compliance_polish.py` has `down_revision` pointing to the 001 revision hash
    - Migration upgrade creates tables `sync_metadata` and `job_runs`, adds column `checked_count` to `compliance_check_runs`
    - Migration downgrade drops both tables and the column
  </acceptance_criteria>
  <done>Three models defined with correct columns, migration file chains from 001 baseline</done>
</task>

<task type="auto">
  <name>Task 2: JobManagerService with DI registration</name>
  <files>app/services/job_manager_service.py, app/container.py</files>
  <read_first>
    - app/container.py (register_services function pattern, lines 115-168)
    - app/services/compliance_checking_service.py (lines 1-35 for service class pattern and imports)
    - .planning/phases/07-compliance-polish/07-PATTERNS.md (JobManagerService pattern section)
  </read_first>
  <action>
    1. Create `app/services/job_manager_service.py`:
       - Import: logging, threading, uuid, concurrent.futures.ThreadPoolExecutor, datetime, timezone, typing (Callable, Dict, Optional, Any)
       - Import: flask current_app, app.database db, app.models.job_run JobRun
       - Define `ConflictError(Exception)` with `__init__(self, message: str, run_id: str)` storing run_id as attribute
       - Define `JobManagerService` class:
         - `__init__`: create `self._executor = ThreadPoolExecutor(max_workers=1)` and `self._lock = threading.Lock()`
         - `start_job(self, job_name: str, runner_fn: Callable, triggered_by: str = "system", app=None) -> str`:
           Acquire self._lock, query JobRun for status="running" + job_name match. If found, raise ConflictError with existing run_id.
           Otherwise create JobRun record (run_id=uuid4 hex, job_name, status="running", triggered_by, started_at=utcnow), commit, release lock.
           Get app reference via `app or current_app._get_current_object()`.
           Submit `self._run_with_context(app, run_id, runner_fn)` to executor. Return run_id.
         - `_run_with_context(self, app, run_id: str, fn: Callable)`:
           Wrap in `with app.app_context():`. Call `fn(run_id=run_id)`. On success: update JobRun status="completed", completed_at=utcnow, commit.
           On exception: update JobRun status="failed", error=str(e)[:500], completed_at=utcnow, commit. Log error with exc_info=True.
         - `get_status(self, run_id: str) -> Optional[Dict[str, Any]]`:
           Query JobRun by run_id. Return None if not found. Otherwise return dict with keys: run_id, job_name, status, started_at (isoformat), completed_at (isoformat or None), error.
         - `is_running(self, job_name: str) -> bool`:
           Query JobRun for status="running" + job_name. Return bool.

    2. Register in `app/container.py` `register_services()` function:
       - Add import: `from app.services.job_manager_service import JobManagerService`
       - Add registration: `container.register("job_manager", lambda c: JobManagerService())`
       - Place after the existing cache_cleanup registration (around line 161)

    Per D-01 (SandCastle job pattern), D-04 (thread-pool executor, mutex, single worker).
  </action>
  <verify>
    <automated>cd /Users/jslitzker/Repos/Who-Dis && python -c "from app.services.job_manager_service import JobManagerService, ConflictError; jm = JobManagerService(); assert hasattr(jm, 'start_job'); assert hasattr(jm, 'get_status'); assert hasattr(jm, 'is_running'); print('JobManagerService OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/services/job_manager_service.py` exports `JobManagerService` and `ConflictError`
    - `JobManagerService.__init__` creates `ThreadPoolExecutor(max_workers=1)` and `threading.Lock()`
    - `start_job` raises `ConflictError` when a job of same name has status="running" in DB
    - `start_job` returns a string run_id (UUID4 format)
    - `_run_with_context` wraps execution in `with app.app_context():`
    - `get_status` returns dict with keys run_id, job_name, status, started_at, completed_at, error
    - `app/container.py` contains `container.register("job_manager", ...)` line
  </acceptance_criteria>
  <done>JobManagerService handles background job lifecycle with mutex, Flask app context, and PostgreSQL state. Registered in DI container as "job_manager".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Job trigger | Admin session or portal M2M token required to start jobs |
| Background thread | Runs with app context but no request context (no g.user available) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | JobManagerService.start_job | mitigate | Callers (Plan 02) enforce @admin_or_portal_required before invoking start_job |
| T-07-02 | Denial of Service | ThreadPoolExecutor | mitigate | Mutex + 409 Conflict response prevents duplicate concurrent runs; max_workers=1 caps resource usage |
| T-07-03 | Tampering | job_runs table | accept | Single-tenant app; no user input goes into job_runs except triggered_by (admin email from g.user) |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase — all stdlib or existing deps |
</threat_model>

<verification>
- `python -c "from app.models.job_run import JobRun; from app.models.sync_metadata import SyncMetadata; print('imports OK')"`
- `python -c "from app.services.job_manager_service import JobManagerService; print('service OK')"`
- `python -c "from app import create_app; app = create_app(); c = app.container; assert c.is_registered('job_manager'); print('DI OK')"`
- Alembic migration file exists and chains from 001 revision
</verification>

<success_criteria>
- All three new files importable without error
- ComplianceCheckRun.checked_count attribute exists
- JobManagerService registered in container
- Migration revision chain is valid (002 -> 001)
</success_criteria>

<output>
Create `.planning/phases/07-compliance-polish/07-01-SUMMARY.md` when done
</output>
