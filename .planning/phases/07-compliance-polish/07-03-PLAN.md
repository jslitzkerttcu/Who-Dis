---
phase: 07-compliance-polish
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - app/templates/admin/partials/_compliance_progress.html
  - app/templates/admin/partials/_warehouse_sync_status.html
  - app/templates/admin/partials/_compliance_violations_table.html
  - app/templates/admin/compliance_dashboard.html
  - app/static/js/compliance-sort.js
  - app/blueprints/admin/job_role_compliance.py
  - app/blueprints/admin/jobs.py
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05]

must_haves:
  truths:
    - "Admin sees live progress bar during bulk compliance check (HTMX polling every 2s)"
    - "Progress bar auto-swaps to results table when check completes"
    - "Violations table is sortable by all columns with severity ranked critical>high>medium>low"
    - "Admin can download CSV of full compliance run with metadata header rows"
    - "Admin sees warehouse sync timestamp and categorized error messages"
    - "Sync Now button triggers warehouse_sync job and disables during run"
  artifacts:
    - path: "app/templates/admin/partials/_compliance_progress.html"
      provides: "HTMX-polling progress bar partial"
      contains: "hx-trigger"
    - path: "app/templates/admin/partials/_warehouse_sync_status.html"
      provides: "Sync status card with timestamp and error display"
      contains: "Sync Now"
    - path: "app/static/js/compliance-sort.js"
      provides: "Client-side table sorting with severity rank map"
      contains: "SEVERITY_RANK"
    - path: "app/blueprints/admin/job_role_compliance.py"
      provides: "CSV export endpoint"
      contains: "compliance_export"
  key_links:
    - from: "app/templates/admin/partials/_compliance_progress.html"
      to: "/api/admin/jobs/compliance_check/status"
      via: "hx-get polling"
      pattern: "hx-get.*status"
    - from: "app/blueprints/admin/job_role_compliance.py"
      to: "app/models/job_role_compliance.py"
      via: "ComplianceCheck query for CSV export"
      pattern: "ComplianceCheck\\.query"
---

<objective>
Build the complete UI layer: progress polling, sync status card, sortable violations table with CSV export, and client-side sorting.

Purpose: Delivers the user-facing experience for all 5 COMP requirements. Admin sees real-time progress, can sort violations, export CSV, and monitor warehouse sync health.
Output: 3 Jinja partials, 1 JS file, CSV export endpoint, enhanced dashboard template, HTMX response handling in jobs status endpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-compliance-polish/07-RESEARCH.md
@.planning/phases/07-compliance-polish/07-PATTERNS.md
@.planning/phases/07-compliance-polish/07-UI-SPEC.md
@.planning/phases/07-compliance-polish/07-01-SUMMARY.md
@.planning/phases/07-compliance-polish/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Progress partial, sync status card, and dashboard wiring</name>
  <files>app/templates/admin/partials/_compliance_progress.html, app/templates/admin/partials/_warehouse_sync_status.html, app/templates/admin/compliance_dashboard.html, app/blueprints/admin/jobs.py</files>
  <read_first>
    - app/templates/admin/partials/_compliance_overview.html (existing partial pattern — Tailwind classes, card structure, HTMX attributes)
    - app/templates/admin/compliance_dashboard.html (current dashboard layout — where to insert progress bar and sync status card)
    - app/blueprints/admin/jobs.py (status endpoint created in Plan 02 — add HTMX response branch)
    - .planning/phases/07-compliance-polish/07-UI-SPEC.md (Progress Bar Specification, Sync Status Card Specification, Copywriting Contract)
  </read_first>
  <action>
    1. Create `app/templates/admin/partials/_compliance_progress.html`:
       - Outer div: id="compliance-progress", hx-get="/api/admin/jobs/compliance_check/status/{{ run_id }}", hx-trigger="every 2s", hx-swap="outerHTML"
       - Progress bar container: w-full bg-gray-200 rounded-full h-2 overflow-hidden
       - Fill bar: bg-blue-600 rounded-full h-2 transition-all duration-300, style="width: {{ percent }}%"
       - Counter text below (flex justify-between items-center mt-2 text-sm text-gray-600):
         - Left: "{{ checked_count }}/{{ total_employees }} employees checked"
         - Right: "{{ percent }}% complete"
       - If error_count > 0: show text-red-600 span "{{ error_count }} errors encountered"
       - Template expects variables: run_id, checked_count, total_employees, percent, error_count

    2. Create `app/templates/admin/partials/_warehouse_sync_status.html`:
       - Card container: bg-white rounded-lg shadow-sm border border-gray-200 p-4
       - Layout: flex items-center justify-between
       - Left side (flex items-center space-x-3):
         - Icon based on state: fas fa-check-circle text-green-600 (success), fas fa-exclamation-triangle text-red-600 (error), fas fa-spinner fa-spin text-blue-600 (syncing), fas fa-clock text-yellow-600 (never/stale)
         - Text: "Last synced: {{ last_success_at | timeago }}" or error message from last_error_message or "Never synced"
       - Right side: "Sync Now" button
         - Style: inline-flex items-center px-3 py-2 border border-gray-300 rounded-md text-sm font-medium text-gray-700 bg-white hover:bg-gray-50
         - hx-post="/api/admin/jobs/warehouse_sync" hx-swap="outerHTML" hx-target="#sync-status-card"
         - Disabled state (per D-10): when syncing is True, add disabled class opacity-50 cursor-not-allowed, text="Syncing..."
       - Template expects variables: last_success_at, last_error_message, last_error_category, syncing (bool)

    3. Modify `app/templates/admin/compliance_dashboard.html`:
       - Add a div with id="compliance-progress" above the violations table area (initially empty or hidden)
       - Add warehouse sync status section: include _warehouse_sync_status.html partial
       - Wire "Run Compliance Check" button: hx-post="/api/admin/jobs/compliance_check", hx-target="#compliance-progress", hx-swap="innerHTML"
       - Button states per UI-SPEC: idle (blue-600 bg, fas fa-play icon), running (disabled, spinner, "Running...")

    4. Modify `app/blueprints/admin/jobs.py` status endpoint to handle HTMX responses:
       - In the GET /api/admin/jobs/<name>/status/<run_id> route:
       - If request.headers.get("HX-Request"):
         - If job_name == "compliance_check":
           - If status == "completed": query ComplianceCheckRun by run_id, load violations, render _compliance_violations_table.html with violations data (this stops polling because no hx-trigger on the results table)
           - If status == "running": compute percent = int((checked_count / total_employees) * 100) if total_employees > 0 else 0, render _compliance_progress.html with run_id, checked_count, total_employees, percent, error_count
           - If status == "failed": render error message partial (inline div with error text, no hx-trigger to stop polling)
         - If job_name == "warehouse_sync":
           - Render _warehouse_sync_status.html with updated state from SyncMetadata
       - Non-HTMX: return JSON (already done in Plan 02)
       - Import ComplianceCheckRun, ComplianceCheck, SyncMetadata as needed

    Per D-03 (progress bar + counter, HTMX every 2s), D-07/D-08 (sync status card), D-10 (button disabled during sync).
  </action>
  <verify>
    <automated>cd /Users/jslitzker/Repos/Who-Dis && grep -l "hx-trigger" app/templates/admin/partials/_compliance_progress.html && grep -l "Sync Now" app/templates/admin/partials/_warehouse_sync_status.html && grep -l "compliance-progress" app/templates/admin/compliance_dashboard.html && echo "Templates OK"</automated>
  </verify>
  <acceptance_criteria>
    - `_compliance_progress.html` contains hx-get with "/api/admin/jobs/compliance_check/status/" and hx-trigger="every 2s"
    - `_compliance_progress.html` displays "{{ checked_count }}/{{ total_employees }} employees checked"
    - `_warehouse_sync_status.html` contains "Sync Now" button with hx-post to warehouse_sync endpoint
    - `_warehouse_sync_status.html` shows disabled state when syncing variable is True
    - `compliance_dashboard.html` has div#compliance-progress and includes sync status partial
    - Jobs blueprint status endpoint returns HTMX partials when HX-Request header is present
    - Completed job returns violations table (no hx-trigger) to stop polling
  </acceptance_criteria>
  <done>Admin sees live progress during compliance check (auto-stops on completion) and warehouse sync status with manual re-sync trigger</done>
</task>

<task type="auto">
  <name>Task 2: Client-side sorting and CSV export</name>
  <files>app/static/js/compliance-sort.js, app/templates/admin/partials/_compliance_violations_table.html, app/blueprints/admin/job_role_compliance.py</files>
  <read_first>
    - app/templates/admin/partials/_compliance_violations_table.html (existing violations table structure — columns, pagination, severity badges)
    - app/static/js/blocked-numbers.js (JS class pattern used in project)
    - app/blueprints/admin/job_role_compliance.py (existing route functions, imports, require_role pattern — add CSV export route)
    - .planning/phases/07-compliance-polish/07-UI-SPEC.md (Sortable Table Headers Specification, CSV Export Button Specification, Copywriting Contract for CSV metadata rows)
  </read_first>
  <action>
    1. Create `app/static/js/compliance-sort.js`:
       - Class `ComplianceSortManager` following blocked-numbers.js class pattern:
         - Constructor: init currentSort = {column: null, direction: "asc"}, call init()
         - Static SEVERITY_RANK = {critical: 4, high: 3, medium: 2, low: 1}
         - init(): call setupEventListeners()
         - setupEventListeners(): querySelectorAll("[data-sort-column]") forEach attach click handler
         - handleSort(event): read data-sort-column (column index), data-sort-type (text/severity/date) from element
           - Toggle direction: if same column clicked, flip asc/desc. If new column, start with desc for severity, asc for others
           - Call sortTable()
           - Update header icons: remove active class from all, add to current. Set icon to fa-sort-up (asc) or fa-sort-down (desc)
         - sortTable(columnIndex, direction, sortType):
           - Get tbody, convert rows to array
           - Sort comparator: for sortType "severity" use SEVERITY_RANK lookup, for "date" use Date.parse, otherwise localeCompare
           - Re-append sorted rows to tbody
       - Initialize on DOMContentLoaded and also on htmx:afterSettle (so sorting works after HTMX swaps in the table)
       - Self-invoking pattern: `document.addEventListener("DOMContentLoaded", () => new ComplianceSortManager())`
       - Also: `document.addEventListener("htmx:afterSettle", () => new ComplianceSortManager())`

    2. Modify `app/templates/admin/partials/_compliance_violations_table.html`:
       - Add data-sort-column="N" and data-sort-type="type" attributes to each th element:
         - Employee (UPN): data-sort-column="0" data-sort-type="text"
         - Job Code: data-sort-column="1" data-sort-type="text"
         - System: data-sort-column="2" data-sort-type="text"
         - Violation Type: data-sort-column="3" data-sort-type="text"
         - Severity: data-sort-column="4" data-sort-type="severity"
         - Detected: data-sort-column="5" data-sort-type="date"
       - Add sort icon span inside each th: fas fa-sort text-gray-400 ml-1 (neutral state)
       - Add cursor-pointer select-none hover:bg-gray-100 transition-colors classes to th elements
       - Add toolbar div above table (flex justify-between items-center mb-4):
         - Left: result count text
         - Right: "Download CSV" button (conditionally visible when run_id is set):
           - a tag with href="/admin/api/compliance-export/{{ run_id }}" download attribute
           - Style: inline-flex items-center px-3 py-2 border border-gray-300 rounded-md text-sm font-medium text-gray-700 bg-white hover:bg-gray-50
           - Icon: fas fa-download mr-2
       - Include script tag: src="{{ url_for('static', filename='js/compliance-sort.js') }}"
       - Default sort on render: severity descending (add class to severity header to indicate active desc sort)

    3. Add CSV export route to `app/blueprints/admin/job_role_compliance.py`:
       - Add imports: csv, io, Response (from flask)
       - New function `api_compliance_export(run_id)` decorated with @require_role("admin"):
         - Query ComplianceCheckRun by run_id, return 404 if not found
         - Create io.StringIO output buffer and csv.writer
         - Write metadata rows per D-13: "Run ID: {run_id}", "Date/Time (UTC): {started_at formatted}", "Scope: {scope or 'All employees'}", "Triggered By: {started_by}"
         - Write blank row separator
         - Write column headers per D-14: "Employee (UPN)", "Job Code", "System", "Expected Role", "Actual Assignment", "Violation Type", "Severity", "Remediation Action"
         - Query ComplianceCheck.query.filter_by(check_run_id=run_id).yield_per(100)
         - For each check: write row with all fields. For actual_assignment use str(bool).lower(). For cells starting with =, +, -, @ prefix with apostrophe (CSV injection prevention per T-07 threat model)
         - Return Response with mimetype="text/csv", Content-Disposition: attachment; filename=compliance_run_{run_id}.csv
       - Register route in admin blueprint: /admin/api/compliance-export/<run_id> GET

    Per D-05 (client-side JS sort), D-06 (all columns sortable), D-11 (export button on results table), D-12 (full run export), D-13 (metadata headers), D-14 (CSV columns).
  </action>
  <verify>
    <automated>cd /Users/jslitzker/Repos/Who-Dis && test -f app/static/js/compliance-sort.js && grep -c "SEVERITY_RANK" app/static/js/compliance-sort.js && grep -c "data-sort-column" app/templates/admin/partials/_compliance_violations_table.html && grep -c "api_compliance_export" app/blueprints/admin/job_role_compliance.py && echo "Sort+Export OK"</automated>
  </verify>
  <acceptance_criteria>
    - `compliance-sort.js` defines SEVERITY_RANK with {critical: 4, high: 3, medium: 2, low: 1}
    - `compliance-sort.js` class handles DOMContentLoaded and htmx:afterSettle events
    - `_compliance_violations_table.html` has data-sort-column attributes on all 6 th elements
    - `_compliance_violations_table.html` has "Download CSV" link with href to /admin/api/compliance-export/{{ run_id }}
    - CSV export route returns Content-Disposition header with filename containing run_id
    - CSV output starts with 4 metadata rows (Run ID, Date/Time, Scope, Triggered By) followed by blank row then column headers
    - CSV data rows have 8 columns matching D-14 spec
    - Cells starting with =, +, -, @ are prefixed with apostrophe (CSV injection prevention)
  </acceptance_criteria>
  <done>Violations table sorts client-side by all columns with severity ranking. CSV export downloads full run with metadata headers and injection-safe cell values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client JS -> Server | Sort is client-only; no untrusted input sent to server |
| CSV download | Admin auth required; output sanitized against CSV injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | CSV export | mitigate | Prefix cells starting with =, +, -, @ with apostrophe to prevent formula injection in spreadsheet apps |
| T-07-09 | Information Disclosure | /admin/api/compliance-export/{run_id} | mitigate | @require_role("admin") decorator; run_id is UUID (unguessable) |
| T-07-10 | Spoofing | HTMX responses | accept | Server-rendered HTML only; no user-controlled content in progress partial templates |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
- `compliance-sort.js` file exists with SEVERITY_RANK constant
- Violations table template has data-sort-column attributes
- CSV export endpoint registered and returns text/csv Content-Type
- Progress partial has hx-trigger="every 2s"
- Sync status card shows "Sync Now" button
- `pytest tests/ -x -q --timeout=30` passes (no regressions)
</verification>

<success_criteria>
- Admin can see real-time progress bar during compliance check (polls every 2s, auto-stops)
- Admin can sort violations table by severity (critical first) and all other columns
- Admin can download CSV with metadata headers and all required columns
- Admin sees last sync timestamp with categorized error messages on failure
- Admin can trigger manual warehouse re-sync via button
</success_criteria>

<output>
Create `.planning/phases/07-compliance-polish/07-03-SUMMARY.md` when done
</output>
