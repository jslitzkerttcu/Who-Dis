---
phase: 13-schema-visualization
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - app/templates/admin/database.html
autonomous: false
requirements:
  - SCHEMA-01
  - SCHEMA-02
must_haves:
  truths:
    - "Admin sees a 'Database Schema' accordion card on the database management page"
    - "Clicking the card header expands it and triggers an HTMX lazy-load of the schema diagram"
    - "The Mermaid erDiagram renders as an SVG inside the expanded card"
    - "Clicking a table node highlights it and all FK-connected tables, dimming unrelated tables to 30% opacity"
    - "Clicking the same table again or clicking the SVG background resets all highlights"
    - "Keyboard users can focus and activate table nodes with Enter/Space"
    - "The diagram container supports scroll-based panning via overflow:auto"
  artifacts:
    - path: "app/templates/admin/database.html"
      provides: "Schema accordion card, Mermaid CDN script, initSchemaInteraction() function, toggleSchema() function"
  key_links:
    - from: "app/templates/admin/database.html"
      to: "/admin/api/database/schema"
      via: "hx-get with hx-trigger=revealed"
      pattern: "hx-get.*database_schema"
    - from: "app/templates/admin/database.html"
      to: "cdn.jsdelivr.net/npm/mermaid"
      via: "script src CDN"
      pattern: "cdn\\.jsdelivr\\.net.*mermaid"
---

<objective>
Add the Schema Visualization accordion card to the database admin page with Mermaid.js rendering and click-to-highlight interaction.

Purpose: Completes the user-facing feature by adding the UI that loads the Mermaid erDiagram from the backend endpoint (built in Plan 01), renders it as interactive SVG via Mermaid.js CDN, and provides click-to-highlight interaction on table nodes per CONTEXT D-06/D-08. This delivers SCHEMA-01 (visible ER diagram) and SCHEMA-02 (interactive SVG with zoom, pan, clickable nodes).

Output: A fully functional Database Schema accordion card on the admin database page with interactive ER diagram.
</objective>

<execution_context>
@/Users/jslitzker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jslitzker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jslitzker/Repos/Who-Dis/.planning/ROADMAP.md
@/Users/jslitzker/Repos/Who-Dis/.planning/STATE.md
@/Users/jslitzker/Repos/Who-Dis/.planning/phases/13-schema-visualization/13-CONTEXT.md
@/Users/jslitzker/Repos/Who-Dis/.planning/phases/13-schema-visualization/13-RESEARCH.md
@/Users/jslitzker/Repos/Who-Dis/.planning/phases/13-schema-visualization/13-PATTERNS.md
@/Users/jslitzker/Repos/Who-Dis/.planning/phases/13-schema-visualization/13-UI-SPEC.md
@/Users/jslitzker/Repos/Who-Dis/.planning/phases/13-schema-visualization/13-01-SUMMARY.md

<interfaces>
<!-- Key patterns from existing codebase -->

From app/templates/admin/database.html (lines 206-231):
Table Statistics accordion card pattern:
- bg-white rounded-2xl shadow-md border border-gray-200 wrapper
- Header: bg-gray-700 text-white px-6 py-4 rounded-t-2xl cursor-pointer hover:bg-gray-600 transition, onclick="toggleTableStats()"
- Chevron icon with id, fas fa-chevron-down transition-transform, rotates via rotate-180 class
- Content div: id with hidden class, contains hx-get with hx-trigger="revealed" hx-swap="innerHTML"
- Loading spinner: animate-spin rounded-full h-8 w-8 border-b-2 matching section color

From app/templates/admin/database.html (lines 255-266):
Toggle JS function pattern:
- function toggleTableStats() with getElementById, classList.contains/remove/add('hidden'), icon rotate-180

From app/templates/admin/database.html (lines 286-296):
htmx:afterSwap event listener pattern on document.body

From 13-RESEARCH.md (Pattern 4):
SVG click-to-highlight: query svg for g[id^="entity-"] OR .entityBox elements, build adjacency from window.__schemaFkMap, toggle opacity 1.0/0.3 on entities, 1.0/0.15 on lines

From 13-RESEARCH.md (Code Examples - Mermaid CDN Include):
mermaid.initialize({ startOnLoad: false, theme: 'default', securityLevel: 'strict', er: { layoutDirection: 'TB', minEntityWidth: 100, minEntityHeight: 75, entityPadding: 15, fontSize: 12 } })
mermaid.run() called on htmx:afterSwap when target is schema-content
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Schema accordion card with Mermaid CDN and interaction JS to database.html</name>
  <files>app/templates/admin/database.html</files>
  <read_first>
    - app/templates/admin/database.html (full file -- executor needs the Table Statistics accordion at lines 206-231 as the exact pattern to replicate, the script block at lines 254-297 for toggle and htmx:afterSwap patterns, and the style block at lines 234-252)
    - .planning/phases/13-schema-visualization/13-UI-SPEC.md (card structure, colors from-indigo-600 to-indigo-700, loading state spinner border-indigo-600, error state retry button bg-indigo-600, accessibility contract for tabindex/role/aria-label/sr-only, interaction contract for highlight pseudocode)
    - .planning/phases/13-schema-visualization/13-PATTERNS.md (accordion card pattern, toggle JS pattern, HTMX lazy loading pattern)
    - .planning/phases/13-schema-visualization/13-RESEARCH.md (Pattern 4: SVG click-to-highlight vanilla JS, Mermaid CDN include code example, Pitfall 2: HTMX swap timing, Pitfall 4: SVG selector fragility, Assumption A2: entity selector pattern, Open Question 2: mermaid.run() Promise)
    - .planning/phases/13-schema-visualization/13-CONTEXT.md (D-06: highlight + dim behavior, D-07: overflow auto for panning, D-08: vanilla JS click handlers, D-09: new section on database page, D-10: HTMX lazy-load)
  </read_first>
  <action>
    Modify app/templates/admin/database.html to add three things:

    **A. Schema Accordion Card (HTML)**
    Insert a new full-width card section AFTER the Table Statistics card (after line 231, before the closing `</div>` of max-w-7xl container at line 232). Add `mb-4` class to the Table Statistics card wrapper div (line 207) so spacing is consistent. The new card follows the exact same accordion pattern as Table Statistics (lines 206-231) with these differences:
    - Card wrapper: `bg-white rounded-2xl shadow-md border border-gray-200 mt-4`
    - Header: `bg-gradient-to-r from-indigo-600 to-indigo-700 text-white px-6 py-4 rounded-t-2xl cursor-pointer hover:from-indigo-500 hover:to-indigo-600 transition duration-150` with `onclick="toggleSchema()"`
    - Icon: `fas fa-project-diagram mr-2` (per UI-SPEC copywriting contract)
    - Title text: "Database Schema" (per UI-SPEC)
    - Chevron: `id="schema-icon"` with `fas fa-chevron-down transition-transform`
    - Content div: `id="schema-content"` with `class="hidden"`
    - Inner div: `hx-get="{{ url_for('admin.database_schema') }}"` with `hx-trigger="revealed"` and `hx-swap="innerHTML"`
    - Loading spinner: `animate-spin rounded-full h-8 w-8 border-b-2 border-indigo-600 mr-3` with text "Loading schema diagram..."

    **B. Mermaid.js CDN Include and Initialization (Script)**
    Add a script tag BEFORE the existing `<script>` block (before line 254) to load Mermaid from CDN:
    - `<script src="https://cdn.jsdelivr.net/npm/mermaid@11.15.0/dist/mermaid.min.js"></script>`
    - Immediately after, a second script tag that calls `mermaid.initialize({ startOnLoad: false, theme: 'default', securityLevel: 'strict', er: { layoutDirection: 'TB', minEntityWidth: 100, minEntityHeight: 75, entityPadding: 15, fontSize: 12 } })`

    **C. JavaScript Functions (inside existing script block)**
    Add to the existing `<script>` block (between lines 254-297):

    1. `toggleSchema()` function: exact same pattern as `toggleTableStats()` but targeting `schema-content` and `schema-icon` element IDs

    2. Update the existing `htmx:afterSwap` event listener (lines 286-296) to ALSO handle schema content. After the existing if-block for cache/optimize results, add: if `evt.detail.target.id === 'schema-content'` or `evt.detail.target.closest('#schema-content')`, then call `mermaid.run()` chained with `.then(function() { setTimeout(initSchemaInteraction, 500); })`. Include a fallback: if `mermaid.run()` does not return a thenable, use `setTimeout(initSchemaInteraction, 1000)` (per RESEARCH Open Question 2).

    3. `initSchemaInteraction()` function implementing click-to-highlight per D-06/D-08:
       - Query `document.querySelector('#schema-diagram-container svg')` -- if null, return
       - Find entity nodes: try `svg.querySelectorAll('g[id^="entity-"]')` first; if empty, try `svg.querySelectorAll('.er.entityBox')`, then `svg.querySelectorAll('.entityBox')` (resilient selectors per RESEARCH Pitfall 4 and Assumption A2)
       - Read `window.__schemaFkMap` for the adjacency map (injected by backend _render_schema_diagram)
       - Track `highlightedTable` variable (null or table name string)
       - For each entity node: set `style.cursor = 'pointer'`, `setAttribute('tabindex', '0')`, `setAttribute('role', 'button')`, add click event listener calling `handleEntityClick(entity)`, add keydown listener for Enter/Space (per UI-SPEC accessibility contract)
       - Add click listener on svg element itself: if target is svg or a rect, call `resetHighlight()`
       - `extractTableName(entity)`: return `entity.id?.replace('entity-', '')` OR the text content of the first `<text>` element inside the entity, trimmed (per RESEARCH Pattern 4)
       - `handleEntityClick(entity)`: extract table name, if same as highlightedTable call resetHighlight and return, else call `highlightTable(tableName)`
       - `highlightTable(tableName)`: set highlightedTable, build allConnected set from fkMap[tableName] plus tableName itself, iterate entities setting opacity 1.0 if in allConnected else 0.3, iterate all path/line elements in svg setting opacity 0.15 for dimmed
       - `resetHighlight()`: set highlightedTable to null, set all entity opacity to 1.0, set all path/line opacity to 1.0
       - Add `outline: 2px solid #2563EB` as focus style on entity nodes (per UI-SPEC accessibility: focus indicator blue-600)
       - Respect `prefers-reduced-motion`: if `window.matchMedia('(prefers-reduced-motion: reduce)').matches`, skip CSS transitions on opacity changes

    Note on D-08 deviation: CONTEXT D-08 references "Mermaid click callbacks" but RESEARCH verified (GitHub API, PR #6985 unmerged) that ER diagrams do NOT support native click callbacks. The implementation uses post-render vanilla JS DOM manipulation instead, which achieves the same user-facing behavior. Use securityLevel 'strict' (not 'loose') since click interaction is entirely post-render JS -- this is safer per RESEARCH Pitfall 3.
  </action>
  <verify>
    <automated>cd /Users/jslitzker/Repos/Who-Dis && grep -c "fa-project-diagram" app/templates/admin/database.html && grep -c "mermaid@11" app/templates/admin/database.html && grep -c "initSchemaInteraction" app/templates/admin/database.html && grep -c "toggleSchema" app/templates/admin/database.html && grep -c "__schemaFkMap" app/templates/admin/database.html</automated>
  </verify>
  <acceptance_criteria>
    - database.html contains a card with "Database Schema" title text and `fa-project-diagram` icon
    - Card header uses `from-indigo-600 to-indigo-700` gradient classes
    - Content div has `id="schema-content"` with `class="hidden"` (collapsed by default)
    - Inner div has `hx-get` pointing to `url_for('admin.database_schema')` with `hx-trigger="revealed"`
    - Loading spinner uses `border-indigo-600` color
    - Mermaid CDN script tag references `cdn.jsdelivr.net/npm/mermaid@11.15.0`
    - `mermaid.initialize` is called with `startOnLoad: false` and `securityLevel: 'strict'`
    - `toggleSchema()` function exists and toggles `schema-content` hidden class and `schema-icon` rotation
    - `htmx:afterSwap` handler calls `mermaid.run()` when target is schema-content
    - `initSchemaInteraction()` function exists and reads `window.__schemaFkMap`
    - Entity nodes receive `tabindex="0"` and `role="button"` attributes
    - Click handler sets dimmed entity opacity to 0.3 and dimmed line opacity to 0.15
    - Background/reset click sets all opacities back to 1.0
    - Keydown handler responds to Enter and Space keys
  </acceptance_criteria>
  <done>Database admin page shows a collapsible Schema card that lazy-loads and renders an interactive Mermaid ER diagram with click-to-highlight and keyboard accessibility</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify schema visualization works end-to-end</name>
  <what-built>
    Interactive ER diagram on the Database Management admin page showing all database tables, columns, types, PK/FK markers, and foreign key relationships. Rendered by Mermaid.js from live PostgreSQL metadata with click-to-highlight interaction.
  </what-built>
  <how-to-verify>
    1. Start the app: `python run.py`
    2. Navigate to http://localhost:5000/admin/database
    3. Scroll down past Table Statistics -- verify a "Database Schema" card with indigo header and fa-project-diagram icon is visible
    4. Click the card header -- verify it expands and shows a loading spinner
    5. Wait for the Mermaid ER diagram to render as SVG -- verify you can see table boxes with column names, types, and PK/FK markers
    6. Count a few tables -- verify they match what you see in the Table Statistics section above
    7. Click any table node -- verify it and its FK-connected tables stay at full opacity while unrelated tables dim to ~30%
    8. Click the same table again -- verify all tables return to full opacity
    9. Click the SVG background -- verify all tables return to full opacity
    10. Try scrolling/panning inside the diagram container -- verify overflow scroll works
    11. Tab to a table node using keyboard -- verify focus ring appears (blue outline)
    12. Press Enter on a focused table -- verify highlight behavior triggers
    13. Collapse and re-expand the accordion -- verify diagram persists or re-loads correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the rendering, interaction, or layout</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN -> browser | Mermaid.js loaded from external CDN; code executes in browser |
| Server HTML -> Mermaid renderer | Server-generated Mermaid text parsed and rendered by Mermaid.js |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | Mermaid.js CDN script | mitigate | Pin exact version (11.15.0) in CDN URL; CSP restricts script sources to cdn.jsdelivr.net only |
| T-13-06 | Tampering | Mermaid text injection (XSS) | mitigate | `securityLevel: 'strict'` in Mermaid config prevents embedded scripts; diagram text comes from pg_catalog system data, not user input |
| T-13-07 | Information Disclosure | Schema metadata in browser | accept | Page is admin-only; schema metadata is visible only to authenticated admins who already have database access |
| T-13-SC | Tampering | npm/CDN dependency | accept | Mermaid is a well-known project (~3M weekly downloads); version pinned; CSP blocks other script sources |
</threat_model>

<verification>
1. Database Schema accordion card visible on /admin/database page
2. Accordion expands to show loading spinner, then rendered SVG ER diagram
3. Diagram shows all public schema tables with columns, types, and PK/FK markers
4. Click-to-highlight dims unrelated tables to ~30% opacity
5. Click-to-reset returns all tables to full opacity
6. Keyboard navigation works with Tab, Enter, Space
7. Scroll panning works inside diagram container
</verification>

<success_criteria>
- Admin can see and interact with a live ER diagram on the database management page
- The diagram is rendered from live PostgreSQL metadata (not static)
- Click-to-highlight and keyboard interaction work as specified
- The accordion lazy-loads content only when expanded
- Loading, error, and empty states are handled gracefully
</success_criteria>

<output>
Create `.planning/phases/13-schema-visualization/13-02-SUMMARY.md` when done
</output>
