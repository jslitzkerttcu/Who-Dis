---
phase: 11-workflow-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/workflow.py
  - alembic/versions/005_workflow_tables.py
  - app/services/workflow_service.py
  - app/container.py
  - tests/factories/workflow.py
  - tests/unit/services/test_workflow_service.py
autonomous: true
requirements:
  - WKFL-01
  - WKFL-02
  - WKFL-03
  - WKFL-04

must_haves:
  truths:
    - "WorkflowService.generate_onboarding() creates a Workflow with items from JobRoleMapping entries for a given job code"
    - "WorkflowService.generate_offboarding() creates a Workflow with reversed role items plus StandardOffboardingItem entries"
    - "WorkflowService.complete_item() records who completed an item and when, and auto-completes the workflow when all items are done/skipped"
    - "WorkflowService.skip_item() requires a reason and records skip_reason, completed_by, completed_at"
    - "WorkflowService.get_dashboard_stats() returns active count, overdue count, completed this month, and average completion days"
  artifacts:
    - path: "app/models/workflow.py"
      provides: "Workflow, WorkflowItem, StandardOffboardingItem models"
      contains: "class Workflow"
    - path: "alembic/versions/005_workflow_tables.py"
      provides: "Alembic migration for workflows, workflow_items, standard_offboarding_items tables"
      contains: "005_workflow_tables"
    - path: "app/services/workflow_service.py"
      provides: "WorkflowService with generation, completion, dashboard queries"
      contains: "class WorkflowService"
    - path: "tests/unit/services/test_workflow_service.py"
      provides: "Unit tests for all WorkflowService methods"
      exports: ["test_generate_onboarding", "test_generate_offboarding", "test_complete_item", "test_skip_item"]
  key_links:
    - from: "app/services/workflow_service.py"
      to: "app/models/workflow.py"
      via: "imports Workflow, WorkflowItem, StandardOffboardingItem"
      pattern: "from app.models.workflow import"
    - from: "app/services/workflow_service.py"
      to: "app/models/job_role_compliance.py"
      via: "imports JobRoleMapping, JobCode"
      pattern: "JobRoleMapping.get_active_mappings_for_job_code"
    - from: "app/container.py"
      to: "app/services/workflow_service.py"
      via: "DI registration"
      pattern: "container.register.*workflow_service"
---

<objective>
Build the data layer and business logic for workflow automation: three SQLAlchemy models (Workflow, WorkflowItem, StandardOffboardingItem), an Alembic migration, the WorkflowService with checklist generation/completion/dashboard methods, DI registration, and comprehensive unit tests.

Purpose: Establishes the entire backend foundation that Plan 02 (routes/templates) will consume. All business logic is testable independently of the HTTP layer.
Output: Models, migration, service, container registration, factories, unit tests.
</objective>

<execution_context>
@/Users/jslitzker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jslitzker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-workflow-automation/11-CONTEXT.md
@.planning/phases/11-workflow-automation/11-RESEARCH.md
@.planning/phases/11-workflow-automation/11-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/models/base.py:
- BaseModel: provides save(commit=True), delete(commit=True), update(**kwargs) methods
- TimestampMixin: provides created_at, updated_at columns (DateTime(timezone=True), server_default)
- JSONDataMixin: provides additional_data JSONB column

From app/models/job_role_compliance.py:
- JobCode.get_active_job_codes() -> List[JobCode]
- JobCode.get_by_job_code(job_code: str) -> Optional[JobCode]
- JobRoleMapping.get_active_mappings_for_job_code(job_code: str) -> List[JobRoleMapping]
  - Each mapping has: mapping_type (required/optional/prohibited), system_role (relationship), system_role.system_name, system_role.role_name
- SystemRole: system_name (String 100), role_name (String 255), role_type (String 50)

From app/models/employee_profiles.py:
- EmployeeProfiles.ukg_job_code: String(50) -- job code for auto-detection

From app/services/base.py:
- BaseConfigurableService: provides _get_config(key, default), config_prefix

From app/container.py (registration pattern):
- container.register("service_name", lambda c: ServiceClass())

From alembic/versions/004_external_api_tokens.py:
- revision: str = "004_external_api_tokens"
- down_revision: Union[str, None] = "003_report_cache"

From tests/factories/job_code.py:
- JobCodeFactory(SQLAlchemyModelFactory) with Meta model=JobCode, sqlalchemy_session=db.session, sqlalchemy_session_persistence="flush"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Models and Alembic Migration</name>
  <files>app/models/workflow.py, alembic/versions/005_workflow_tables.py</files>
  <read_first>
    - app/models/job_role_compliance.py (JobCode, SystemRole, JobRoleMapping model patterns, relationship declarations, classmethod query helpers)
    - app/models/base.py (BaseModel, TimestampMixin, JSONDataMixin — import paths and mixin signatures)
    - alembic/versions/004_external_api_tokens.py (migration pattern: revision chain, op.create_table, op.create_index, downgrade)
    - app/database.py (db instance import path)
  </read_first>
  <action>
    Create app/models/workflow.py with three model classes:

    1. Workflow(BaseModel, TimestampMixin): __tablename__ = "workflows".
       Columns: workflow_type (String(20), nullable=False, index=True — values "onboarding"/"offboarding"), status (String(20), nullable=False, default="active", index=True — values "active"/"completed"/"cancelled"), employee_name (String(255), nullable=False), employee_email (String(255), index=True — nullable for net-new hires per D-03), job_code (String(50), nullable=False, index=True), job_title (String(255)), created_by (String(255), nullable=False, index=True), completed_at (DateTime(timezone=True)).
       Relationship: items = db.relationship("WorkflowItem", back_populates="workflow", cascade="all, delete-orphan", order_by="WorkflowItem.sort_order").
       Classmethods: get_active_workflows() returns Workflow.query.filter_by(status="active").order_by(Workflow.created_at.desc()).all(). get_completed_workflows() returns filter_by(status="completed") ordered by completed_at desc.
       Property: progress — returns dict with total (len items), completed (count status in completed/skipped), pending (count status pending), percent (int 0-100).
       Property: overdue_count — returns count of items where status=="pending" and due_date is not None and due_date < date.today().

    2. WorkflowItem(BaseModel, TimestampMixin): __tablename__ = "workflow_items".
       Columns: workflow_id (Integer, ForeignKey("workflows.id"), nullable=False, index=True), item_text (String(500), nullable=False — denormalized copy per RESEARCH pitfall 4), item_source (String(50), nullable=False — values "role_mapping"/"standard_offboarding"), source_detail (String(255) — e.g. "keystone.TellerRole (required)"), action_type (String(20), nullable=False — "add"/"remove"/"action"), system_name (String(100)), role_name (String(255)), sort_order (Integer, nullable=False, default=0), status (String(20), nullable=False, default="pending", index=True — "pending"/"completed"/"skipped"), completed_by (String(255)), completed_at (DateTime(timezone=True)), skip_reason (Text), due_date (Date).
       Relationship: workflow = db.relationship("Workflow", back_populates="items").
       Property: is_overdue — returns True if status=="pending" and due_date is not None and due_date < date.today().

    3. StandardOffboardingItem(BaseModel, TimestampMixin): __tablename__ = "standard_offboarding_items".
       Columns: item_text (String(500), nullable=False), sort_order (Integer, nullable=False, default=0), is_active (Boolean, default=True, index=True), created_by (String(255), nullable=False).
       Classmethod: get_all_active() returns query.filter_by(is_active=True).order_by(cls.sort_order).all().

    Create alembic/versions/005_workflow_tables.py:
       revision = "005_workflow_tables", down_revision = "004_external_api_tokens".
       upgrade(): op.create_table for all three tables with matching columns, then op.create_index calls for: ix_workflows_status, ix_workflows_workflow_type, ix_workflows_employee_email, ix_workflows_job_code, ix_workflows_created_by, ix_workflow_items_workflow_id, ix_workflow_items_status, ix_standard_offboarding_items_is_active.
       downgrade(): drop indexes then drop tables in reverse order (standard_offboarding_items, workflow_items, workflows).
  </action>
  <verify>
    <automated>cd /Users/jslitzker/Repos/Who-Dis && python -c "from app.models.workflow import Workflow, WorkflowItem, StandardOffboardingItem; print('Models import OK')" && python -c "from alembic.versions import __path__; print('Migration exists')"</automated>
  </verify>
  <acceptance_criteria>
    - app/models/workflow.py contains class Workflow(BaseModel, TimestampMixin) with __tablename__ = "workflows"
    - app/models/workflow.py contains class WorkflowItem(BaseModel, TimestampMixin) with __tablename__ = "workflow_items"
    - app/models/workflow.py contains class StandardOffboardingItem(BaseModel, TimestampMixin) with __tablename__ = "standard_offboarding_items"
    - WorkflowItem.workflow_id has ForeignKey("workflows.id")
    - WorkflowItem has item_source column with values documented as "role_mapping" or "standard_offboarding"
    - Workflow.items relationship uses cascade="all, delete-orphan" and order_by="WorkflowItem.sort_order"
    - StandardOffboardingItem has get_all_active() classmethod
    - Workflow has progress property returning dict with total, completed, pending, percent keys
    - Workflow has overdue_count property
    - WorkflowItem has is_overdue property
    - alembic/versions/005_workflow_tables.py has revision = "005_workflow_tables" and down_revision = "004_external_api_tokens"
    - Migration upgrade() creates three tables: workflows, workflow_items, standard_offboarding_items
    - Migration downgrade() drops all three tables
  </acceptance_criteria>
  <done>Three model classes importable from app.models.workflow; Alembic migration 005 creates and can drop all three tables</done>
</task>

<task type="auto">
  <name>Task 2: WorkflowService, DI Registration, Test Factories, and Unit Tests</name>
  <files>app/services/workflow_service.py, app/container.py, tests/factories/workflow.py, tests/unit/services/test_workflow_service.py</files>
  <read_first>
    - app/models/workflow.py (just created in Task 1 — Workflow, WorkflowItem, StandardOffboardingItem classes and their methods)
    - app/services/compliance_checking_service.py (service class pattern: BaseConfigurableService, @handle_service_errors, batch save with commit=False, aggregate queries with func.count)
    - app/services/job_role_mapping_service.py (CRUD service pattern, audit logging calls)
    - app/container.py (registration pattern: container.register with lambda factory)
    - tests/factories/job_code.py (factory_boy SQLAlchemyModelFactory pattern: Meta class, session=db.session, persistence="flush")
    - tests/factories/system_role.py (SystemRoleFactory pattern)
    - tests/factories/job_role_mapping.py (JobRoleMappingFactory pattern — uses SubFactory for job_code and system_role)
    - tests/unit/services/test_compliance_checking_service.py (test structure: pytestmark, fixtures, factory usage, db_session fixture)
    - app/models/job_role_compliance.py (JobRoleMapping.get_active_mappings_for_job_code — the core query used by generation)
  </read_first>
  <action>
    Create app/services/workflow_service.py with class WorkflowService(BaseConfigurableService):
       __init__: super().__init__(config_prefix="workflow")
       Module-level logger = logging.getLogger(__name__).

       Methods (all decorated with @handle_service_errors(raise_errors=True)):

       generate_onboarding(self, employee_name: str, employee_email: Optional[str], job_code: str, created_by: str) -> Workflow:
         Query JobRoleMapping.get_active_mappings_for_job_code(job_code). Raise ValueError if no mappings. Get JobCode.get_by_job_code(job_code) for job_title. Create Workflow(workflow_type="onboarding", ..., created_by=created_by). save(commit=False). Iterate mappings: skip mapping_type=="prohibited". For each valid mapping create WorkflowItem with item_text="Assign: {role_name} ({system_name})" for required, "Consider assigning: {role_name} ({system_name})" for optional. Set item_source="role_mapping", source_detail="{system_name}.{role_name} ({mapping_type})", action_type="add", system_name and role_name from mapping.system_role, sort_order=enumerate index. save(commit=False). db.session.commit(). Return workflow.

       generate_offboarding(self, employee_name: str, employee_email: Optional[str], job_code: str, created_by: str) -> Workflow:
         Same mapping query. Create Workflow(workflow_type="offboarding"). For each non-prohibited mapping: item_text="Remove: {role_name} ({system_name})", action_type="remove". Then query StandardOffboardingItem.get_all_active() and append as WorkflowItems with item_source="standard_offboarding", action_type="action", sort_order continuing from role items. db.session.commit(). Return workflow.

       complete_item(self, item_id: int, completed_by: str) -> WorkflowItem:
         Query WorkflowItem by id. Raise ValueError if not found or item.status != "pending". Set status="completed", completed_by, completed_at=datetime.now(timezone.utc). save(). Call _check_workflow_completion(item.workflow). Return item.

       skip_item(self, item_id: int, completed_by: str, reason: str) -> WorkflowItem:
         Raise ValueError if not reason or reason.strip() is empty (per D-06 required note). Query item, check status=="pending". Set status="skipped", completed_by, completed_at=datetime.now(timezone.utc), skip_reason=reason.strip(). save(). Call _check_workflow_completion(item.workflow). Return item.

       cancel_workflow(self, workflow_id: int) -> Workflow:
         Query Workflow by id. Set status="cancelled", completed_at=now. save(). Return workflow.

       get_dashboard_stats(self) -> Dict[str, Any]:
         active_count: Workflow.query.filter_by(status="active").count().
         overdue_count: db.session.query(func.count(WorkflowItem.id)).join(Workflow).filter(Workflow.status=="active", WorkflowItem.status=="pending", WorkflowItem.due_date < date.today(), WorkflowItem.due_date.isnot(None)).scalar() or 0.
         completed_this_month: count Workflows where status=="completed" and completed_at >= first day of current month.
         avg_completion_days: avg of (completed_at - created_at) for completed workflows, or 0.
         Return dict with keys: active, overdue, completed_this_month, avg_completion_days.

       get_active_workflows(self) -> List[Workflow]:
         Return Workflow.get_active_workflows().

       get_completed_workflows(self, page: int = 1, per_page: int = 20):
         Return paginated completed workflows using Workflow.query.filter_by(status="completed").order_by(Workflow.completed_at.desc()).paginate(page=page, per_page=per_page).

       get_workflow(self, workflow_id: int) -> Optional[Workflow]:
         Return Workflow.query.get(workflow_id).

       _check_workflow_completion(self, workflow: Workflow) -> None:
         If all items have status in ("completed", "skipped"): set workflow.status="completed", workflow.completed_at=datetime.now(timezone.utc), workflow.save().

    Register in app/container.py: add import from app.services.workflow_service import WorkflowService, then container.register("workflow_service", lambda c: WorkflowService()) after the existing external_api_token_service registration block.

    Create tests/factories/workflow.py with three factories: WorkflowFactory, WorkflowItemFactory, StandardOffboardingItemFactory. Follow JobCodeFactory pattern exactly (SQLAlchemyModelFactory, Meta model, session=db.session, persistence="flush"). WorkflowFactory: workflow_type="onboarding", status="active", employee_name=Sequence, employee_email=LazyAttribute from name, job_code="JC0001", created_by="admin@test.com". WorkflowItemFactory: workflow=SubFactory(WorkflowFactory), item_text=Sequence, item_source="role_mapping", action_type="add", sort_order=0, status="pending". StandardOffboardingItemFactory: item_text=Sequence, sort_order=0, is_active=True, created_by="admin@test.com".

    Update tests/factories/__init__.py to export the new factories.

    Create tests/unit/services/test_workflow_service.py with pytestmark = pytest.mark.unit. Fixture svc(app, db_session) returns WorkflowService(). Tests:
       test_generate_onboarding: create JobCodeFactory + SystemRoleFactory + JobRoleMappingFactory (2 required, 1 optional, 1 prohibited), call generate_onboarding, assert workflow created with 3 items (prohibited excluded), correct item_text prefixes, action_type="add".
       test_generate_onboarding_no_mappings: call with nonexistent job code, assert ValueError raised.
       test_generate_offboarding: same setup + create 2 StandardOffboardingItemFactory, call generate_offboarding, assert items include role removals (item_text starts with "Remove:") + standard items (item_source="standard_offboarding").
       test_complete_item: create workflow + item, call complete_item, assert status="completed", completed_by set, completed_at set.
       test_complete_item_already_completed: complete then try again, assert ValueError.
       test_skip_item_requires_reason: call skip_item with empty reason, assert ValueError.
       test_skip_item: call skip_item with valid reason, assert status="skipped", skip_reason set.
       test_auto_complete_workflow: create workflow with 2 items, complete both, assert workflow.status=="completed" and workflow.completed_at is not None.
       test_dashboard_stats: create 2 active workflows, 1 completed workflow (completed_at in current month), 1 overdue item (due_date in past). Call get_dashboard_stats, assert active==2, overdue==1, completed_this_month==1, avg_completion_days is numeric.
       test_cancel_workflow: create workflow, cancel, assert status=="cancelled".
  </action>
  <verify>
    <automated>cd /Users/jslitzker/Repos/Who-Dis && python -m pytest tests/unit/services/test_workflow_service.py -x -v --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - app/services/workflow_service.py contains class WorkflowService(BaseConfigurableService)
    - WorkflowService has methods: generate_onboarding, generate_offboarding, complete_item, skip_item, cancel_workflow, get_dashboard_stats, get_active_workflows, get_completed_workflows, get_workflow, _check_workflow_completion
    - generate_onboarding raises ValueError when no mappings found for job code
    - generate_offboarding appends StandardOffboardingItem entries after role mapping items
    - skip_item raises ValueError when reason is empty or whitespace-only
    - complete_item raises ValueError when item.status is not "pending"
    - _check_workflow_completion sets workflow.status="completed" when all items are completed or skipped
    - app/container.py contains container.register("workflow_service", lambda c: WorkflowService())
    - tests/factories/workflow.py contains WorkflowFactory, WorkflowItemFactory, StandardOffboardingItemFactory
    - tests/unit/services/test_workflow_service.py has at least 10 test functions
    - pytest tests/unit/services/test_workflow_service.py exits 0 with all tests passing
  </acceptance_criteria>
  <done>WorkflowService registered in DI container; all unit tests pass covering onboarding/offboarding generation, item completion/skip, auto-complete, dashboard stats, and cancellation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin UI -> WorkflowService | Admin-submitted form data (employee_name, email, job_code, skip_reason) crosses into business logic |
| WorkflowService -> PostgreSQL | ORM queries execute against database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing | workflow routes | mitigate | @auth_required + @require_role("admin") on all endpoints (Plan 02) |
| T-11-02 | Tampering | employee_name, skip_reason inputs | mitigate | SQLAlchemy parameterized queries (ORM) + Jinja2 auto-escaping; validate string length in service layer |
| T-11-03 | Repudiation | item completion/skip | mitigate | audit_service.log_admin_action() on every state change (Plan 02 routes) |
| T-11-04 | Tampering | CSRF on POST endpoints | mitigate | @csrf_double_submit.protect + hx-headers CSRF token (Plan 02) |
| T-11-05 | Elevation of Privilege | workflow manipulation by non-admin | mitigate | @require_role("admin") on all workflow routes |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; all dependencies already in requirements.txt |
</threat_model>

<verification>
1. All three models importable: `python -c "from app.models.workflow import Workflow, WorkflowItem, StandardOffboardingItem"`
2. Migration runs: `cd /Users/jslitzker/Repos/Who-Dis && alembic upgrade head` (if DB available)
3. Service registered: `python -c "from app import create_app; app = create_app(); ctx = app.app_context(); ctx.push(); svc = app.container.get('workflow_service'); print(type(svc))"`
4. Unit tests green: `python -m pytest tests/unit/services/test_workflow_service.py -x -v`
</verification>

<success_criteria>
- Workflow, WorkflowItem, StandardOffboardingItem models defined with correct columns, relationships, and helper methods
- Alembic migration 005 creates and drops all three tables
- WorkflowService handles all checklist generation, completion tracking, and dashboard stat computation
- Container registers workflow_service
- 10+ unit tests pass covering all WKFL requirements at the service layer
</success_criteria>

<output>
Create `.planning/phases/11-workflow-automation/11-01-SUMMARY.md` when done
</output>
