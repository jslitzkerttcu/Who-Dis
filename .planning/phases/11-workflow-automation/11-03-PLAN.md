---
phase: 11-workflow-automation
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - app/templates/admin/workflow_offboarding_items.html
  - app/blueprints/admin/workflows.py
  - app/blueprints/admin/__init__.py
  - app/templates/search/partials/_profile_card.html
autonomous: false
requirements:
  - WKFL-01
  - WKFL-02
  - WKFL-04

must_haves:
  truths:
    - "Admin can manage standard offboarding items (add, edit, delete, reorder) via /admin/workflows/offboarding-items"
    - "Start Onboarding button appears on expanded profile cards for admin users"
    - "Clicking Start Onboarding navigates to create workflow form pre-populated with employee data"
    - "Standard offboarding items are included in every offboarding checklist"
  artifacts:
    - path: "app/templates/admin/workflow_offboarding_items.html"
      provides: "Admin UI for managing standard offboarding items"
      contains: "Standard Offboarding Items"
    - path: "app/templates/search/partials/_profile_card.html"
      provides: "Start Onboarding button on profile cards"
      contains: "Start Onboarding"
  key_links:
    - from: "app/templates/search/partials/_profile_card.html"
      to: "/admin/workflows/create"
      via: "href with employee_email query param"
      pattern: "url_for.*workflows.*create.*employee_email"
    - from: "app/blueprints/admin/workflows.py"
      to: "app/models/workflow.py"
      via: "StandardOffboardingItem CRUD"
      pattern: "StandardOffboardingItem"
---

<objective>
Complete the workflow automation feature by adding standard offboarding items management (D-10/D-11), the "Start Onboarding" button on search profile cards (D-03 search-first path), and a human verification checkpoint for the full workflow.

Purpose: Delivers the remaining pieces needed for complete WKFL-01/02/04 compliance: configurable offboarding items (D-10/D-11), search integration (D-03), and visual verification.
Output: Offboarding items admin page, profile card button, additional routes, end-of-phase verification.
</objective>

<execution_context>
@/Users/jslitzker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jslitzker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-workflow-automation/11-CONTEXT.md
@.planning/phases/11-workflow-automation/11-UI-SPEC.md
@.planning/phases/11-workflow-automation/11-01-SUMMARY.md
@.planning/phases/11-workflow-automation/11-02-SUMMARY.md

<interfaces>
<!-- Key types from prior plans -->

From app/models/workflow.py (Plan 01):
- StandardOffboardingItem: item_text, sort_order, is_active, created_by, created_at
- StandardOffboardingItem.get_all_active() -> List[StandardOffboardingItem]

From app/blueprints/admin/workflows.py (Plan 02):
- Already has route handlers; this plan adds offboarding item management routes
- create_workflow handles GET with query param employee_email for pre-population

From app/templates/admin/workflow_offboarding_items.html:
- Needs: list of items, inline add form, edit/delete per item, reorder arrows

From search profile card:
- Existing profile card template shows expanded employee data
- Needs "Start Onboarding" button visible only to admin users
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standard Offboarding Items Admin and Search Integration</name>
  <files>app/templates/admin/workflow_offboarding_items.html, app/blueprints/admin/workflows.py, app/blueprints/admin/__init__.py, app/templates/search/partials/_profile_card.html</files>
  <read_first>
    - app/blueprints/admin/workflows.py (current state from Plan 02 — existing route handlers)
    - app/blueprints/admin/__init__.py (current state — existing route wiring)
    - app/blueprints/admin/api_tokens.py (CRUD pattern for inline add/delete with HTMX)
    - app/templates/admin/_external_api_tokens.html (inline form + list management template pattern)
    - app/templates/search/partials/_profile_card.html (current profile card template — where to add Start Onboarding button)
    - app/models/workflow.py (StandardOffboardingItem model — fields and methods)
    - .planning/phases/11-workflow-automation/11-UI-SPEC.md (offboarding items page spec and profile card button spec)
  </read_first>
  <action>
    Add routes to app/blueprints/admin/workflows.py for standard offboarding items CRUD:

    manage_offboarding_items(): GET /admin/workflows/offboarding-items. @require_role("admin"). Query StandardOffboardingItem.query.order_by(StandardOffboardingItem.sort_order).all() (include inactive for management view). Render "admin/workflow_offboarding_items.html" with items.

    add_offboarding_item(): POST /admin/workflows/offboarding-items/add. @require_role("admin") @csrf_double_submit.protect. Get item_text from form. Validate non-empty and length <= 500. Create StandardOffboardingItem with created_by=g.user, sort_order=max existing + 1. save(). Audit log action="offboarding_item_created". Return HTMX fragment: the new item row appended to list (hx-swap="beforeend" on the list).

    delete_offboarding_item(item_id): POST /admin/workflows/offboarding-items/<int:item_id>/delete. @require_role("admin") @csrf_double_submit.protect. Get item, set is_active=False (soft delete per RESEARCH). save(). Audit log action="offboarding_item_deleted". Return empty response with HX-Trigger "itemDeleted" for HTMX to remove the row.

    update_offboarding_item(item_id): POST /admin/workflows/offboarding-items/<int:item_id>/update. @require_role("admin") @csrf_double_submit.protect. Get item_text from form. Update item.item_text. save(). Audit log. Return updated item row fragment.

    reorder_offboarding_items(): POST /admin/workflows/offboarding-items/reorder. @require_role("admin") @csrf_double_submit.protect. Get item_ids list from form (ordered). Update sort_order for each. db.session.commit(). Return full items list partial.

    Wire new routes in app/blueprints/admin/__init__.py:
      admin_bp.route("/workflows/offboarding-items")(workflows.manage_offboarding_items)
      admin_bp.route("/workflows/offboarding-items/add", methods=["POST"])(workflows.add_offboarding_item)
      admin_bp.route("/workflows/offboarding-items/<int:item_id>/delete", methods=["POST"])(workflows.delete_offboarding_item)
      admin_bp.route("/workflows/offboarding-items/<int:item_id>/update", methods=["POST"])(workflows.update_offboarding_item)
      admin_bp.route("/workflows/offboarding-items/reorder", methods=["POST"])(workflows.reorder_offboarding_items)

    Create app/templates/admin/workflow_offboarding_items.html per UI-SPEC:
      Extends base.html. Page title "Standard Offboarding Items". Breadcrumb: Admin > Workflows > Offboarding Items. Description text explaining these items appear in every offboarding checklist. List container div id="offboarding-items-list". Each item: bg-white border border-gray-200 rounded-md px-4 py-3 flex items-center justify-between. Left: item text. Right: up/down arrow buttons (hx-post to reorder), edit button (toggles inline edit form), delete button (window.confirm per UI-SPEC: "Remove this standard offboarding item? It will no longer appear in new offboarding checklists." then hx-post to delete). Inline add form at bottom: text input + "Add Item" button. hx-post to add endpoint, hx-target="#offboarding-items-list", hx-swap="beforeend". Include CSRF token via hx-headers.

    Update app/templates/search/partials/_profile_card.html:
      Find the actions/buttons area of the expanded profile card. Add a conditional block: if g.role == "admin" or g.user_role == "admin" (check whichever attribute the template uses for role checking). Inside the conditional: a button/link styled per UI-SPEC: inline-flex items-center px-3 py-1.5 border border-gray-300 shadow-sm text-xs font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50. Icon fa-clipboard-list mr-1.5. Text "Start Onboarding". href="{{ url_for('admin.create_workflow', employee_email=result.email) }}" (or however the profile card accesses the employee email from the search result context). This links directly to the create form with the employee pre-populated.
  </action>
  <verify>
    <automated>cd /Users/jslitzker/Repos/Who-Dis && python -c "from app.blueprints.admin.workflows import manage_offboarding_items, add_offboarding_item, delete_offboarding_item; print('Offboarding routes OK')" && grep -c "Start Onboarding" app/templates/search/partials/_profile_card.html</automated>
  </verify>
  <acceptance_criteria>
    - app/blueprints/admin/workflows.py contains manage_offboarding_items, add_offboarding_item, delete_offboarding_item, update_offboarding_item, reorder_offboarding_items functions
    - All offboarding item routes have @require_role("admin") and @csrf_double_submit.protect (on POST handlers)
    - add_offboarding_item validates item_text is non-empty
    - delete_offboarding_item sets is_active=False (soft delete) not hard delete
    - All state-changing offboarding routes call audit_logger.log_admin_action
    - app/blueprints/admin/__init__.py contains 5 new route registrations for offboarding items
    - app/templates/admin/workflow_offboarding_items.html has div id="offboarding-items-list" and inline add form with hx-post
    - Delete confirmation uses window.confirm with text matching UI-SPEC
    - app/templates/search/partials/_profile_card.html contains "Start Onboarding" text
    - Start Onboarding button is conditional on admin role
    - Start Onboarding href includes employee_email query parameter to url_for("admin.create_workflow")
    - grep -c "Start Onboarding" app/templates/search/partials/_profile_card.html returns at least 1
  </acceptance_criteria>
  <done>Standard offboarding items fully manageable via admin UI; Start Onboarding button appears on profile cards for admins linking to pre-populated create form</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete workflow automation feature: dashboard with KPI cards, onboarding/offboarding checklist generation from job role mappings, per-item completion/skip tracking with audit trail, standard offboarding items admin, and profile card integration.</what-built>
  <how-to-verify>
    1. Start the application: python run.py
    2. Navigate to /admin/ -- verify "Workflows" card appears in the admin grid with fa-list-check icon
    3. Click into Workflows -- verify KPI cards show (Active Workflows, Overdue Items, Completed This Month, Avg Completion Time)
    4. Click "New Workflow" -- verify create form with employee search typeahead and job code dropdown
    5. Search for an existing employee -- verify typeahead results appear and selecting one pre-fills fields
    6. Toggle "Employee not in any system yet?" -- verify manual entry fields appear
    7. Select a job code and verify the preview panel shows items that will be generated
    8. Toggle between Onboarding/Offboarding types -- verify preview updates
    9. Generate an onboarding checklist -- verify redirect to detail page with all items from role mappings
    10. Mark an item as completed -- verify green check appears, "Completed by" attribution shows
    11. Skip an item -- verify yellow skip state with reason displayed
    12. Try to skip without a reason -- verify error message appears
    13. Navigate to /admin/workflows/offboarding-items -- verify standard offboarding items management page
    14. Add, edit, and delete a standard offboarding item -- verify HTMX updates work
    15. Generate an offboarding checklist -- verify it includes role removals AND standard offboarding items
    16. Return to dashboard -- verify active workflow count and progress are correct
    17. Search for an employee in the search page -- verify "Start Onboarding" button on expanded profile card
    18. Check audit logs (/admin/audit-logs) -- verify entries for workflow creation, item completion, skip
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> offboarding item CRUD | Admin-submitted offboarding item text |
| Search results -> create form | Employee email passed via query parameter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering | offboarding item text input | mitigate | Jinja2 auto-escaping; validate length <= 500 in route handler |
| T-11-07 | Elevation of Privilege | Start Onboarding button | mitigate | Button only visible to admin role; create_workflow route has @require_role("admin") |
| T-11-08 | Tampering | CSRF on offboarding item CRUD | mitigate | @csrf_double_submit.protect on all POST handlers |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
1. Offboarding items CRUD: navigate to /admin/workflows/offboarding-items, add/edit/delete items
2. Profile card button: search for an employee, expand profile, verify "Start Onboarding" button (admin only)
3. Pre-population: click "Start Onboarding", verify create form has employee data pre-filled
4. Offboarding generation includes standard items: generate offboarding checklist, verify standard items appear in a separate section
5. Full suite: python -m pytest tests/ -x -q --timeout=30
</verification>

<success_criteria>
- Standard offboarding items are fully manageable via admin UI (add, edit, delete, reorder)
- Start Onboarding button appears on profile cards for admin users only
- Clicking Start Onboarding pre-populates the create workflow form
- Offboarding checklists include both role removals and standard items
- Human verification confirms all 18 checklist items pass
</success_criteria>

<output>
Create `.planning/phases/11-workflow-automation/11-03-SUMMARY.md` when done
</output>
