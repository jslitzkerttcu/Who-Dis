---
phase: 08-reporting
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - app/blueprints/admin/reports.py
  - app/templates/admin/partials/_report_genesys.html
  - app/templates/admin/partials/_report_history.html
  - app/blueprints/admin/__init__.py
autonomous: true
requirements: [REPT-05, REPT-06, REPT-07, REPT-08]

must_haves:
  truths:
    - "Contact Center tab shows live Genesys agent presence, routing status, and queue memberships"
    - "Contact Center tab lazy-loads via hx-trigger=revealed when tab opens"
    - "Run History tab shows job execution history (name, started, completed, duration, status)"
    - "Genesys tab has its own CSV export button"
    - "Run History shows status badges (success/failed/running)"
    - "REPT-06 satisfied via SandCastle portal scheduling + WhoDis history display"
  artifacts:
    - path: "app/templates/admin/partials/_report_genesys.html"
      provides: "Contact center presence table with status dots"
      contains: "Contact Center Status"
    - path: "app/templates/admin/partials/_report_history.html"
      provides: "Job run history table with status badges"
      contains: "Report Run History"
    - path: "app/blueprints/admin/reports.py"
      provides: "Genesys and history tab route handlers + genesys CSV export"
      contains: "def api_genesys_tab"
  key_links:
    - from: "app/blueprints/admin/reports.py"
      to: "app/services/genesys_service.py"
      via: "container.get('genesys_service')"
      pattern: "genesys_service"
    - from: "app/blueprints/admin/reports.py"
      to: "app/services/job_manager_service.py"
      via: "container.get('job_manager')"
      pattern: "job_manager"
---

<objective>
Complete the reports section with the Contact Center (Genesys presence) tab and Run History tab, plus Genesys CSV export.

Purpose: Delivers REPT-05 (Genesys agent status), REPT-06/07 (schedule management via portal + history view), and completes the tabbed reports interface.
Output: Genesys tab partial, history tab partial, route handlers, CSV export, full 4-tab reporting section.
</objective>

<execution_context>
@/Users/jslitzker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jslitzker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-reporting/08-RESEARCH.md
@.planning/phases/08-reporting/08-PATTERNS.md
@.planning/phases/08-reporting/08-CONTEXT.md
@.planning/phases/08-reporting/08-UI-SPEC.md
@.planning/phases/08-reporting/08-01-SUMMARY.md
@.planning/phases/08-reporting/08-02-SUMMARY.md

<interfaces>
<!-- From Plans 01 and 02 output -->

From app/services/genesys_service.py (Plan 01 addition):
def get_all_agents_presence(self) -> List[Dict[str, Any]]:
    # Returns list of dicts with: id, name, email, presence, routingStatus, queues

From app/services/job_manager_service.py:
class JobManagerService:
    def get_all_runs(self) -> List[Dict]:  # or similar history query method
    def get_status(self, run_id: str) -> Optional[Dict]: ...

From app/blueprints/admin/jobs.py:
JOB_REGISTRY = [...]  # Contains job metadata (name, display_name)

From app/blueprints/admin/reports.py (Plan 02):
# Already has: reports(), _render_tab(), _render_licenses_tab(), _render_security_tab()
# Already has: export_license_csv(), export_security_csv(), _csv_safe()
# Plan 03 adds: api_genesys_tab(), api_history_tab(), _render_genesys_tab(), _render_history_tab(), export_genesys_csv()

From app/models/report_cache.py (Plan 01):
class ReportCache - is_stale, age_display, get_cached(), store()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Genesys tab route, history tab route, Genesys CSV export, and route registration</name>
  <files>
    app/blueprints/admin/reports.py
    app/blueprints/admin/__init__.py
  </files>
  <read_first>
    app/blueprints/admin/reports.py
    app/blueprints/admin/__init__.py
    app/services/genesys_service.py
    app/services/job_manager_service.py
    app/blueprints/admin/jobs.py
  </read_first>
  <action>
**Extend reports.py** — add to the _render_tab dispatcher dict: "genesys" -> _render_genesys_tab, "history" -> _render_history_tab.

**Genesys tab handler** — _render_genesys_tab():
Get genesys_service from container. Call get_all_agents_presence(). Pass agents list to template admin/partials/_report_genesys.html. No caching (live per D-09). Handle service returning empty list gracefully.

**api_genesys_tab():**
@require_role("admin"). Thin wrapper calling _render_genesys_tab(). This is the named route target for hx-get.

**History tab handler** — _render_history_tab():
Get job_manager from container. Query run history — call job_manager.get_history() or equivalent method that returns list of recent job runs. If no such method exists on JobManagerService, query the underlying storage directly (check if job_runs table/model exists, or if history is kept in-memory in the job_manager). Filter to report-related jobs only (job names starting with "report_"). Pass runs list to template admin/partials/_report_history.html. Per D-14 (read-only history).

**api_history_tab():**
@require_role("admin"). Thin wrapper calling _render_history_tab().

**Genesys CSV export** — export_genesys_csv():
@require_role("admin"). Get genesys_service, call get_all_agents_presence(). Build CSV with metadata rows: "Report: Contact Center Status", "Generated: {now}", blank row. Headers: Agent Name, Email, Presence, Routing Status, Queues. Data rows from agents list, queues joined as comma-separated string. Apply _csv_safe to name and email fields. Return CSV Response with Content-Disposition. Per D-04.

**Route registration** (app/blueprints/admin/__init__.py):
Add three new routes after the existing report routes:
admin_bp.route("/api/reports/genesys")(reports.api_genesys_tab)
admin_bp.route("/api/reports/history")(reports.api_history_tab)
admin_bp.route("/api/reports/export/genesys")(reports.export_genesys_csv)
  </action>
  <verify>
    <automated>cd /Users/jslitzker/Repos/Who-Dis && python -c "
from app.blueprints.admin.reports import api_genesys_tab, api_history_tab, export_genesys_csv
assert callable(api_genesys_tab)
assert callable(api_history_tab)
assert callable(export_genesys_csv)
print('ALL CHECKS PASSED')
" && ruff check app/blueprints/admin/reports.py app/blueprints/admin/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - _render_tab dispatcher handles "genesys" and "history" keys
    - api_genesys_tab() has @require_role("admin") and calls genesys_service.get_all_agents_presence()
    - api_history_tab() has @require_role("admin") and queries job run history
    - History tab filters to report-related jobs (names starting with "report_")
    - export_genesys_csv() returns CSV with Agent Name, Email, Presence, Routing Status, Queues columns
    - All three routes registered in admin __init__.py
    - ruff check passes
  </acceptance_criteria>
  <done>Genesys and history tab routes functional, Genesys CSV export working, all routes registered</done>
</task>

<task type="auto">
  <name>Task 2: Genesys and Run History tab templates</name>
  <files>
    app/templates/admin/partials/_report_genesys.html
    app/templates/admin/partials/_report_history.html
  </files>
  <read_first>
    app/templates/admin/partials/_compliance_violations_table.html
    app/templates/admin/partials/_report_licenses.html
    app/blueprints/admin/reports.py
    .planning/phases/08-reporting/08-UI-SPEC.md
  </read_first>
  <action>
**_report_genesys.html** — Contact Center tab partial:

Status bar: flex row with "Contact Center Status" heading (text-xl font-semibold), "Export CSV" button on right (links to /admin/api/reports/export/genesys).

No stale badge for Genesys (live data per D-09).

Agent table: min-w-full divide-y divide-gray-200. Headers: Agent Name, Presence, Routing Status, Queues (with data-sort-column for client-side sorting). Each row:
- Agent Name: text-sm font-semibold text-gray-900
- Presence: colored dot (w-2 h-2 rounded-full inline-block mr-2) + status text. Color mapping per UI-SPEC:
  - "ON_QUEUE" / "On Queue": bg-green-500
  - "AVAILABLE" / "Available": bg-green-400
  - "AWAY" / "BREAK": bg-yellow-400
  - "BUSY": bg-red-400
  - "OFFLINE" / other: bg-gray-400
- Routing Status: text-sm text-gray-700
- Queues: comma-separated list, text-sm text-gray-600

Empty state: if agents list is empty, show centered empty state with fa-headset icon, heading "No Genesys Agents Found", body per UI-SPEC copywriting.

Loading state: The tab uses hx-trigger="revealed" so initial state should be a loading spinner (from reports.html). The partial itself is the loaded content.

**_report_history.html** — Run History tab partial:

Heading: "Report Run History" (text-xl font-semibold mb-4).

Table: min-w-full divide-y divide-gray-200. Headers: Job Name, Started, Completed, Duration, Status.

Rows iterate over runs list. Each row:
- Job Name: display_name from job metadata (look up by name in JOB_REGISTRY or pass display_name from route handler)
- Started: formatted timestamp (e.g., "May 17, 2026 04:00 AM")
- Completed: formatted timestamp or "-" if still running
- Duration: calculated difference (e.g., "2m 15s") or "-" if still running
- Status: pill badge per UI-SPEC:
  - "completed": bg-green-100 text-green-800 "Success"
  - "failed": bg-red-100 text-red-800 "Failed"
  - "running": bg-blue-100 text-blue-800 "Running" with animate-spin spinner icon

Empty state: if no runs, show centered div with fa-clock-rotate-left icon, heading "No Report Runs Yet", body per UI-SPEC copywriting.

Both templates follow the same structural pattern as _report_licenses.html (created in Plan 02).
  </action>
  <verify>
    <automated>cd /Users/jslitzker/Repos/Who-Dis && ls app/templates/admin/partials/_report_genesys.html app/templates/admin/partials/_report_history.html && grep -l "Contact Center Status" app/templates/admin/partials/_report_genesys.html && grep -l "Report Run History" app/templates/admin/partials/_report_history.html</automated>
  </verify>
  <acceptance_criteria>
    - _report_genesys.html contains a table with Agent Name, Presence, Routing Status, Queues columns
    - Presence column shows colored dot (bg-green-500/400, bg-yellow-400, bg-red-400, bg-gray-400) based on status
    - Export CSV button links to /admin/api/reports/export/genesys
    - Empty state shows "No Genesys Agents Found" heading with fa-headset icon
    - _report_history.html contains a table with Job Name, Started, Completed, Duration, Status columns
    - Status column uses pill badges: bg-green-100 text-green-800 for success, bg-red-100 text-red-800 for failed, bg-blue-100 text-blue-800 for running
    - Running status pill includes animate-spin spinner icon
    - Empty state shows "No Report Runs Yet" heading with fa-clock-rotate-left icon
    - data-sort-column attributes present on Genesys table headers
  </acceptance_criteria>
  <done>Both tab templates complete with proper styling, status indicators, empty states, and export buttons</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Genesys API -> template | Untrusted external presence data rendered in HTML |
| Admin browser -> tab routes | Admin-only; no user input beyond tab selection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Information Disclosure | Genesys agent data | accept | Admin-only access; presence data is low-sensitivity operational info |
| T-08-10 | Tampering | Genesys API response rendered in template | mitigate | Jinja2 autoescaping prevents XSS from agent names/queue names |
| T-08-11 | Tampering | Genesys CSV export | mitigate | _csv_safe() applied to all string fields before CSV write |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan |
</threat_model>

<verification>
- GET /admin/api/reports/genesys returns HTML partial (requires admin session + Genesys configured)
- GET /admin/api/reports/history returns HTML partial with run history
- GET /admin/api/reports/export/genesys returns CSV response
- Templates render cleanly even with empty data (empty states shown)
- All 4 tabs accessible from /admin/reports via tab navigation
</verification>

<success_criteria>
All four report tabs (Licenses, Security, Contact Center, Run History) are functional. Admin can view live Genesys presence data, see job run history, and export Genesys data as CSV. The complete reporting section satisfies all 8 REPT requirements.
</success_criteria>

<output>
Create `.planning/phases/08-reporting/08-03-SUMMARY.md` when done
</output>
