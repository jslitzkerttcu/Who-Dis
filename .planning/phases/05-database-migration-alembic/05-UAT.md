---
status: complete
phase: 05-database-migration-alembic
source: [.planning/ROADMAP.md §"Phase 5", .planning/phases/05-database-migration-alembic/05-CONTEXT.md, .planning/PR-25-AUDIT.md §"Phase 5"]
started: 2026-04-26T20:30:43Z
updated: 2026-04-26T20:32:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Stop any running container/server. Start the app fresh (e.g. `docker compose up` or `./docker-entrypoint.sh`). The container boots without errors, `alembic upgrade head` runs and completes before gunicorn binds (visible in startup logs), and the homepage / health endpoint returns a live response backed by Postgres.
result: pass
ref: docker-entrypoint.sh:31, alembic/env.py

### 2. WD-DB-01 — DATABASE_URL is the only connection string
expected: The app reads `DATABASE_URL` exclusively. There are no remaining references to `POSTGRES_HOST`, `POSTGRES_PORT`, `POSTGRES_DB`, `POSTGRES_USER`, `POSTGRES_PASSWORD` for assembling a connection string in `app/database.py` or any code path that builds the SQLAlchemy URI. `.env.example` shows `DATABASE_URL=...` and not the legacy split vars.
result: pass
ref: app/database.py (Plan 03-02 — commits 8b90fb2, fc44ccf, 9bdceaf), .env.example

### 3. WD-DB-02 — Alembic auto-applies on container start
expected: `docker-entrypoint.sh` runs `alembic upgrade head` before `exec gunicorn ...`. `alembic/versions/001_baseline_from_live_schema.py` contains the autogenerated DDL captured from the live schema (not an empty stub). `alembic.ini` has `sqlalchemy.url` blank and `alembic/env.py` reads `DATABASE_URL` from the environment.
result: pass
ref: docker-entrypoint.sh:31, alembic/versions/001_baseline_from_live_schema.py (commit 01b291c), alembic.ini, alembic/env.py

### 4. WD-DB-03 — SandCastle data-migration runbook
expected: An operator-facing runbook exists at `docs/runbooks/sandcastle-data-migration.md` (or equivalent, e.g. `scripts/cutover/README.md` per CONTEXT.md). It walks an operator through: dump current DB → provision SandCastle DB via `provision-db.sh whodis` → restore data → verify row counts. Companion scripts in `scripts/cutover/` (`migrate_secrets_to_portal.py`, `restore_to_sandcastle.sh`, `dump_live_schema.sh`, `seed_keycloak_admins.py`) are present and referenced.
result: pass
ref: scripts/cutover/README.md, scripts/cutover/*.{py,sh}

### 5. WD-DB-04 — SQLAlchemy pool tuned for containers
expected: `app/database.py` configures the SQLAlchemy engine with `pool_size=5`, `pool_pre_ping=True`, plus complementary settings (`pool_recycle=1800`, `max_overflow=5`). Pool exhaustion produces structured log output (not a raw stack trace) when capacity is exceeded.
result: pass
ref: app/database.py:41-46

### 6. WD-DB-05 — Schema change roundtrip without manual psql
expected: A developer can author a new Alembic revision (`alembic revision --autogenerate -m "..."`), commit it, push through the deploy/portal-webhook path, and on container restart `alembic upgrade head` applies it automatically. No step in the documented flow requires an operator to run `psql` against the live DB by hand.
result: pass
ref: docker-entrypoint.sh, alembic/, deploy webhook docs

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0

## Gaps

[none yet]
