# Phase 5: Database Migration & Alembic - Context (TRIAGE STUB)

**Gathered:** 2026-04-26
**Status:** Triage stub — refine via `/gsd-discuss-phase 5` before planning

> ⚠ **This is a triage stub created during the Phase 3 cross-phase audit, not a full discussion.** PR #25 already shipped Phase 5 work; this file captures the audit findings and pending closure decision so a future `/gsd-discuss-phase 5` session can refine, not rebuild.

<domain>
## Phase Boundary

Move schema management to Alembic, switch the app to a single `DATABASE_URL`, document the one-time data-migration runbook for Who-Dis → SandCastle Postgres. Tune the connection pool for containerized deployment. Delivers WD-DB-01..05 (5 requirements).

**Special framing — gap closure, not greenfield:** PR #25 (`fdb6ff2`, "Phase 9 SandCastle onboarding") + post-merge baseline-population fix `01b291c` shipped 4 of 5 WD-DB requirements. The remaining gap (WD-DB-01) is the same `DATABASE_URL` refactor that Phase 3 closes via Plan 03-02 — so Phase 5 has zero net work after Plan 03-02 lands.

</domain>

<canonical_refs>
## Canonical References

### Single source of truth — START HERE
- `.planning/PR-25-AUDIT.md` §"Phase 5 — Database Migration & Alembic" — Per-requirement audit. **§"Gap Closure — G1" contains the DATABASE_URL refactor decisions (D-G1-01..05) that close WD-DB-01 via Plan 03-02.**

### Project Planning
- `.planning/ROADMAP.md` §"Phase 5: Database Migration & Alembic" — 5 success criteria
- `.planning/REQUIREMENTS.md` §"SandCastle — Database" — WD-DB-01..05

### Existing Code (already shipped — do NOT redesign)
- `alembic.ini` — Migration config (sqlalchemy.url intentionally blank; env.py overrides)
- `alembic/env.py` — Reads `DATABASE_URL` from `os.environ` at migration time
- `alembic/versions/001_baseline_from_live_schema.py` — Baseline migration body populated by `01b291c` (autogenerated DDL captures the live schema)
- `docker-entrypoint.sh:31` — `alembic upgrade head` before gunicorn (WD-DB-02)
- `app/database.py:41-46` — Pool tuning (WD-DB-04: pool_size=5, pool_recycle=1800, pool_pre_ping=True, max_overflow=5)
- `scripts/cutover/README.md` — Operator runbook for one-time data migration (WD-DB-03)
- `scripts/cutover/{migrate_secrets_to_portal.py, restore_to_sandcastle.sh, dump_live_schema.sh, seed_keycloak_admins.py}` — Cutover toolkit

### Cross-phase dependency
- **WD-DB-01 is closed by Phase 3 Plan 03-02 (`DATABASE_URL` refactor in `app/database.py`).** Plan 03-02 must merge before Phase 5 verification can run.

</canonical_refs>

<decisions>
## Implementation Decisions

### From the cross-phase audit (locked)
- **D-01:** Phase 5 is **verify-only** — 4/5 requirements verified done by audit; the 5th (WD-DB-01) is closed by Phase 3 Plan 03-02 (cross-phase satisfaction per `PR-25-AUDIT.md` D-G1-03).
- **D-02:** No 05-* PLAN.md files needed. Phase 5 closes by:
  1. Waiting for Phase 3 Plan 03-02 to merge (closes WD-DB-01)
  2. Running `/gsd-verify-work 5` to produce `05-VERIFICATION.md`
  3. Marking WD-DB-01..05 complete in REQUIREMENTS.md
- **D-03:** **Single retroactive `05-VERIFICATION.md`** scores all 5 WD-DB requirements with file/line evidence + commit SHA references (`fdb6ff2`, `01b291c`, plus `<03-02 SHA>` for WD-DB-01).

### Pending — refine via `/gsd-discuss-phase 5`
- Whether to add an `alembic revision --autogenerate -m "verify_clean"` smoke test in CI to catch model/schema drift (per the verification note in `001_baseline_from_live_schema.py:T-09-04-03`)
- Whether the WD-DB-03 operator runbook needs a "rehearsal" appendix (cutover simulation against a copy)
- Whether to delete the old `database/create_tables.sql` references from any documentation (`scripts/`, `docs/`) that may still mention them

</decisions>

<deferred>
## Deferred Ideas

- **CI auto-migration drift check** — A `pytest` smoke test that runs `alembic upgrade head` against a clean test DB then `alembic revision --autogenerate -m "verify"` and asserts the diff is empty. Could become a Phase 2 follow-up or a CI-pipeline phase.
- **Cutover rehearsal runbook** — Worth adding before any future schema change that touches the live DB; not needed for Phase 5 closure.
- **Schema visualization tool** — `eralchemy` or `schemacrawler` to generate ER diagrams from the live metadata. Backlog material.

</deferred>

---

*Phase: 05-database-migration-alembic*
*Context gathered: 2026-04-26 (triage stub)*
