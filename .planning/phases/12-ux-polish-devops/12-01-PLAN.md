---
phase: 12-ux-polish-devops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/sku_catalog_cache.py
  - app/blueprints/search/__init__.py
  - app/templates/search/_m365_section.html
autonomous: true
requirements: [UXP-01]
must_haves:
  truths:
    - "User hovers over any SKU badge on a profile card and sees a tooltip listing the top 5 humanized service plan names"
    - "Tooltip shows '+N more service plans' count when more than 5 plans exist"
    - "Badge with no service plan data degrades gracefully (no tooltip shown, no error)"
    - "Existing admin remove button on badges still works and does not conflict with tooltip hover"
  artifacts:
    - path: "app/services/sku_catalog_cache.py"
      provides: "get_service_plans() method returning humanized plan names from raw_data JSONB"
      contains: "def get_service_plans"
    - path: "app/services/sku_catalog_cache.py"
      provides: "SERVICE_PLAN_FRIENDLY_NAMES dict and _humanize_service_plan function"
      contains: "SERVICE_PLAN_FRIENDLY_NAMES"
    - path: "app/blueprints/search/__init__.py"
      provides: "service_plans key added to each license dict in _build_m365_section_data"
      contains: "service_plans"
    - path: "app/templates/search/_m365_section.html"
      provides: "Tailwind tooltip component replacing title attribute on license badges"
      contains: "role=\"tooltip\""
  key_links:
    - from: "app/templates/search/_m365_section.html"
      to: "app/blueprints/search/__init__.py"
      via: "lic.get('service_plans') dict passed through template context"
      pattern: "service_plans"
    - from: "app/blueprints/search/__init__.py"
      to: "app/services/sku_catalog_cache.py"
      via: "sku_catalog.get_service_plans(sku_id) call"
      pattern: "get_service_plans"
    - from: "app/services/sku_catalog_cache.py"
      to: "app/models/external_service.py"
      via: "ExternalServiceData.get_by_service_id() to read raw_data JSONB"
      pattern: "get_by_service_id"
---

<objective>
Add service plan tooltip to license SKU badges on profile cards. When a user hovers over a license badge, a styled Tailwind tooltip appears showing the top 5 humanized service plan names included in that license, with a "+N more" count if applicable.

Purpose: Implements UXP-01 -- users can understand what is inside each license without leaving the WhoDis interface or cross-referencing Microsoft admin portals.
Output: Working tooltip on all license badges, backed by service plan data extracted from existing SkuCatalogCache raw_data JSONB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ux-polish-devops/12-CONTEXT.md
@.planning/phases/12-ux-polish-devops/12-RESEARCH.md
@.planning/phases/12-ux-polish-devops/12-PATTERNS.md
@.planning/phases/12-ux-polish-devops/12-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/services/sku_catalog_cache.py:
- class SkuCatalogCache(BaseConfigurableService) at line 26
- def get_sku_name(self, sku_id: str) -> Optional[str] at line 115 -- analog for new method
- Uses ExternalServiceData.get_name_by_id("graph", "sku", sku_id)
- raw_data stored as full Graph SKU JSON including servicePlans array (line 90-101)

From app/models/external_service.py:
- ExternalServiceData.get_by_service_id(service_name, data_type, service_id) -> Optional[ExternalServiceData] at line 55
- Returns model with .raw_data attribute (JSONB column)

From app/blueprints/search/__init__.py:
- def _build_m365_section_data(user_profile, mfa_result, sku_catalog) at line 828
- License dict construction at lines 886-908 (loop over assignedLicenses, call sku_catalog.get_sku_name)
- sku_catalog obtained via current_app.container.get("sku_catalog") at line 747

From app/templates/search/_m365_section.html:
- License badge loop at lines 106-140
- Current: title="{{ lic.get('skuId') }}" -- to be replaced with tooltip div
- Admin remove button uses group-hover:opacity-100 at line 135
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add service plan extraction and humanization to SkuCatalogCache</name>
  <files>app/services/sku_catalog_cache.py</files>
  <read_first>
    app/services/sku_catalog_cache.py
    app/models/external_service.py
    .planning/phases/12-ux-polish-devops/12-RESEARCH.md
    .planning/phases/12-ux-polish-devops/12-PATTERNS.md
  </read_first>
  <action>
    Add a module-level dict SERVICE_PLAN_FRIENDLY_NAMES mapping ~40-50 common Microsoft service plan technical names to friendly display names (per RESEARCH.md Pattern 2). Include at minimum: EXCHANGE_S_ENTERPRISE, TEAMS1, SHAREPOINTENTERPRISE, OFFICESUBSCRIPTION, MCOSTANDARD, INTUNE_A, AAD_PREMIUM, AAD_PREMIUM_P2, RMS_S_ENTERPRISE, MFA_PREMIUM, POWERAPPS_O365_P2, FLOW_O365_P2, PROJECTWORKMANAGEMENT, SWAY, YAMMER_ENTERPRISE, Deskless, STREAM_O365_E3, FORMS_PLAN_E3, WHITEBOARD_PLAN1, KAIZALA_O365_P3, MICROSOFTBOOKINGS, OFFICE_FORMS_PLAN_2, MYANALYTICS_P2, M365_LIGHTHOUSE_PARTNER_PLAN1, CDS_O365_P2, POWER_VIRTUAL_AGENTS_O365_P2, EXCHANGE_S_STANDARD, SHAREPOINTSTANDARD, MCOSTANDARD, TEAMS_FREE, AAD_BASIC, INFORMATION_BARRIERS, CONTENT_EXPLORER, DLP, ATP_ENTERPRISE, THREAT_INTELLIGENCE.

    Add a module-level function _humanize_service_plan(plan_name: str) -> str that looks up plan_name in SERVICE_PLAN_FRIENDLY_NAMES; if not found, falls back to replacing underscores with spaces and title-casing, stripping trailing version numbers like _P1, _P2, _E3 before title-casing.

    Add a method get_service_plans(self, sku_id: str, limit: int = 5) -> dict to SkuCatalogCache. It should:
    1. Call ExternalServiceData.get_by_service_id("graph", "sku", sku_id)
    2. If entry is None or entry.raw_data is None, return {"plans": [], "total": 0}
    3. Extract raw_plans from entry.raw_data.get("servicePlans", [])
    4. Filter to plans where appliesTo == "User" and provisioningStatus == "Success"
    5. Sort by a PRIORITY_PLANS list (Exchange, Teams, SharePoint, Office apps first), then alphabetically for the rest
    6. Humanize each plan name via _humanize_service_plan
    7. Return {"plans": humanized[:limit], "total": len(filtered_plans)}

    Follow the error handling pattern from get_sku_name -- wrap in try/except, log errors with logger.error, return {"plans": [], "total": 0} on failure. Per D-01, D-03, D-04.
  </action>
  <verify>
    <automated>python -c "from app.services.sku_catalog_cache import SkuCatalogCache, SERVICE_PLAN_FRIENDLY_NAMES, _humanize_service_plan; assert len(SERVICE_PLAN_FRIENDLY_NAMES) >= 30; assert _humanize_service_plan('EXCHANGE_S_ENTERPRISE') == 'Exchange Online (Plan 2)'; assert _humanize_service_plan('UNKNOWN_PLAN_X') != 'UNKNOWN_PLAN_X'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - SERVICE_PLAN_FRIENDLY_NAMES dict exists at module level with at least 30 entries
    - _humanize_service_plan("EXCHANGE_S_ENTERPRISE") returns "Exchange Online (Plan 2)"
    - _humanize_service_plan("SOME_UNKNOWN_THING") returns a title-cased humanized string, not the raw technical name
    - SkuCatalogCache has method get_service_plans(self, sku_id: str, limit: int = 5) -> dict
    - get_service_plans returns {"plans": list_of_strings, "total": int}
    - get_service_plans filters to appliesTo=="User" and provisioningStatus=="Success"
    - On error or missing data, get_service_plans returns {"plans": [], "total": 0}
    - ruff check app/services/sku_catalog_cache.py passes
    - mypy app/services/sku_catalog_cache.py passes
  </acceptance_criteria>
  <done>SkuCatalogCache.get_service_plans() extracts and humanizes service plan names from stored raw_data JSONB. Fallback humanization handles unknown plans gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Wire service plans into license dicts and render tooltip in template</name>
  <files>app/blueprints/search/__init__.py, app/templates/search/_m365_section.html</files>
  <read_first>
    app/blueprints/search/__init__.py
    app/templates/search/_m365_section.html
    .planning/phases/12-ux-polish-devops/12-UI-SPEC.md
    .planning/phases/12-ux-polish-devops/12-PATTERNS.md
  </read_first>
  <action>
    In app/blueprints/search/__init__.py, inside _build_m365_section_data() at the license dict construction loop (lines 886-908), after the line that sets "skuId": sku_id in the license dict, add a service_plans lookup. Follow the same try/except pattern used for get_sku_name (lines 897-901):

    Call sku_catalog.get_service_plans(sku_id) if sku_catalog is not None, store result as service_plans_data. On exception, log debug and set service_plans_data to {"plans": [], "total": 0}. Add "service_plans": service_plans_data to the license dict appended to the licenses list. Per D-01, D-04.

    ALSO: Find the second location where licenses are built (around line 1157 in the compare view) and apply the same service_plans injection there.

    In app/templates/search/_m365_section.html, replace the license badge loop (lines 106-140) per the UI-SPEC tooltip component specification:

    1. Change the outer span class from "relative group" to "relative group/badge" to namespace the hover group (prevents conflict with admin button group-hover per PATTERNS.md)
    2. Remove title="{{ lic.get('skuId') }}" attribute
    3. Add aria-describedby="tooltip-{{ loop.index }}" when service_plans data exists
    4. Insert the tooltip div between the display name text and the admin remove button:
       - id="tooltip-{{ loop.index }}" with role="tooltip"
       - Position: absolute left-1/2 -translate-x-1/2 top-full mt-2
       - Style: bg-gray-900 text-gray-100 rounded-md px-2 py-2 min-w-[200px] max-w-[280px] text-[11px]
       - Visibility: opacity-0 invisible group-hover/badge:opacity-100 group-hover/badge:visible
       - Transition: transition-opacity duration-150 ease-in-out z-50
       - Pointer: pointer-events-none group-hover/badge:pointer-events-auto
       - Arrow: div with absolute -top-1.5 left-1/2 -translate-x-1/2 w-3 h-3 bg-gray-900 rotate-45
       - Heading: font-semibold text-white text-xs mb-1 showing lic displayName
       - List: ul with space-y-0.5, each li has flex items-start gap-1, bullet span text-gray-400, plan name span
       - Overflow: calculate remaining = total - plans|length, show "+N more service plans" div in text-gray-400 mt-1
       - Empty state: if service_plans exists but plans list is empty, show "No service plan details available"
    5. Update admin remove button hover to use group-hover/badge:opacity-100 instead of group-hover:opacity-100
    6. If service_plans key is missing or falsy on a license, do NOT render tooltip (graceful degradation per UI-SPEC error state). Per D-02, D-03.
  </action>
  <verify>
    <automated>cd /Users/jslitzker/Repos/Who-Dis && python -c "
from app import create_app
app = create_app()
with app.app_context():
    from app.blueprints.search import _build_m365_section_data
    # Test with a mock sku_catalog that has get_service_plans
    class MockCatalog:
        def get_sku_name(self, sid): return 'Test License'
        def get_service_plans(self, sid, limit=5): return {'plans': ['Exchange Online'], 'total': 1}
    profile = {'assignedLicenses': [{'skuId': 'test-id', 'disabledPlans': []}]}
    data = _build_m365_section_data(profile, None, MockCatalog())
    lics = data.get('licenses', [])
    assert len(lics) == 1
    assert 'service_plans' in lics[0]
    assert lics[0]['service_plans']['plans'] == ['Exchange Online']
    print('Blueprint wiring OK')
" && grep -c 'role="tooltip"' app/templates/search/_m365_section.html && grep -c 'group/badge' app/templates/search/_m365_section.html</automated>
  </verify>
  <acceptance_criteria>
    - _build_m365_section_data returns license dicts containing "service_plans" key with {"plans": [...], "total": int}
    - app/templates/search/_m365_section.html contains role="tooltip" on tooltip div
    - app/templates/search/_m365_section.html uses group/badge class on badge span (not bare group)
    - Template uses group-hover/badge:opacity-100 for tooltip visibility
    - Admin remove button updated to group-hover/badge:opacity-100
    - title="{{ lic.get('skuId') }}" no longer appears in the badge span
    - Tooltip shows "+N more service plans" when remaining > 0
    - Empty state text "No service plan details available" appears when plans list is empty but service_plans exists
    - No tooltip rendered when service_plans key is missing (graceful degradation)
    - ruff check app/blueprints/search/__init__.py passes
    - grep -c 'aria-describedby' app/templates/search/_m365_section.html returns at least 1
  </acceptance_criteria>
  <done>License badges show styled Tailwind tooltip on hover with humanized service plan names, "+N more" overflow, and graceful degradation for missing data. Admin remove button hover is unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Graph API cache -> template | Service plan names from cached Graph API data rendered in HTML |
| User hover interaction -> tooltip display | CSS-only visibility toggle, no server round-trip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Spoofing | Service plan names in tooltip | accept | Data comes from Microsoft Graph API (trusted source), stored in DB cache -- not user-controllable |
| T-12-02 | Tampering | XSS via service plan names in tooltip | mitigate | Jinja2 auto-escaping is enabled by default in Flask; all {{ }} expressions are HTML-escaped. No |safe filter used on plan names |
| T-12-03 | Information Disclosure | Service plan details visible to all authenticated users | accept | License info is already visible on profile cards; service plan breakdown adds detail but no sensitive data |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan -- all work uses existing dependencies |
</threat_model>

<verification>
1. Run ruff check on modified Python files: ruff check app/services/sku_catalog_cache.py app/blueprints/search/__init__.py
2. Run mypy on modified Python files: mypy app/services/sku_catalog_cache.py app/blueprints/search/__init__.py
3. Verify tooltip HTML structure: grep -c 'role="tooltip"' app/templates/search/_m365_section.html returns >= 1
4. Verify group/badge namespacing: grep -c 'group/badge' app/templates/search/_m365_section.html returns >= 1
5. Verify old title attribute removed: grep -c 'title="{{ lic.get' app/templates/search/_m365_section.html returns 0
</verification>

<success_criteria>
- SkuCatalogCache.get_service_plans() returns humanized service plan names from raw_data JSONB
- _build_m365_section_data() includes service_plans in license dicts
- License badges show styled Tailwind tooltip on hover with up to 5 humanized plan names
- "+N more service plans" shown when total exceeds displayed count
- Graceful degradation when no service plan data available
- Admin remove button on badges unaffected by tooltip addition
- ruff and mypy pass on all modified files
</success_criteria>

<output>
Create `.planning/phases/12-ux-polish-devops/12-01-SUMMARY.md` when done
</output>
