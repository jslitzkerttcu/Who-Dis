---
phase: 12-ux-polish-devops
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified: []
autonomous: false
requirements: [UXP-01, DEVOPS-01, DEVOPS-02, DEVOPS-03]
must_haves:
  truths:
    - "User confirms tooltip displays correctly on license badges"
    - "User confirms Docker image builds and runs successfully"
  artifacts: []
  key_links: []
---

<objective>
Verify both Phase 12 workstreams visually and functionally: (1) SKU license tooltip renders correctly on profile cards, (2) Docker multi-stage build produces a working, smaller image.

Purpose: Human verification of visual correctness (tooltip styling, positioning, content) and Docker build success (image size reduction, container starts correctly).
Output: Verified phase completion, ready for /gsd:verify-work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-ux-polish-devops/12-01-SUMMARY.md
@.planning/phases/12-ux-polish-devops/12-02-SUMMARY.md
@.planning/phases/12-ux-polish-devops/12-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    1. SKU license tooltip: Hovering over any license badge on a profile card now shows a styled dark tooltip with the top 5 humanized service plan names and a "+N more" count.
    2. Docker multi-stage build: Dockerfile rewritten with builder and runtime stages. Healthcheck uses Python urllib instead of curl. .dockerignore excludes .planning/.
  </what-built>
  <how-to-verify>
    **Tooltip Verification:**
    1. Start the app: python run.py
    2. Navigate to http://localhost:5000 and log in
    3. Search for a user who has M365 licenses assigned
    4. Hover over any license badge (e.g., "Microsoft 365 E3") on their profile card
    5. Verify: A dark tooltip appears below the badge showing:
       - License name as a bold heading
       - Up to 5 service plan names (e.g., "Exchange Online (Plan 2)", "Microsoft Teams")
       - "+N more service plans" if the license has more than 5 plans
    6. Verify: Moving the mouse away dismisses the tooltip
    7. Verify: The admin remove button (X) on badges still appears on hover (if you are admin)
    8. Verify: Badges for licenses with no service plan data show no tooltip (no error)

    **Docker Verification:**
    1. Record current image size: docker images whodis (or build current Dockerfile from git history)
    2. Build new image: docker build -t whodis:v4-test .
    3. Check new image size: docker images whodis:v4-test
    4. Verify at least 30% size reduction (per D-07, document before/after in response)
    5. Run container: docker run --rm whodis:v4-test python -c "import flask; import pyodbc; print('imports OK')"
    6. Verify .planning/ is not in image: docker run --rm whodis:v4-test ls -la /app/.planning 2>&1 (should show "No such file or directory")
  </how-to-verify>
  <resume-signal>Type "approved" with before/after Docker image sizes, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan, no code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | -- | -- | -- | Verification-only checkpoint, no new attack surface |
</threat_model>

<verification>
Human confirms both workstreams function correctly per the how-to-verify steps above.
</verification>

<success_criteria>
- Tooltip displays correctly on license badges with humanized service plan names
- Admin remove button on badges unaffected
- Docker build succeeds with multi-stage Dockerfile
- Docker image is at least 30% smaller than current (documented before/after sizes)
- Container imports flask and pyodbc successfully
- .planning/ directory not present in runtime image
</success_criteria>

<output>
Create `.planning/phases/12-ux-polish-devops/12-03-SUMMARY.md` when done
</output>
