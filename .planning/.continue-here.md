---
context: default
phase: production-bugfix-session
task: null
total_tasks: null
status: paused
last_updated: 2026-05-16T07:00:00.000Z
---

<current_state>
Production bug-fix session complete. 9 fixes deployed to main. App is live at whodis.sandcastle.ttcu.com. Phases 1-6 verified complete in ROADMAP. Next planned phase is 7 (Compliance Polish). Three operational items remain before resuming roadmap work.
</current_state>

<completed_work>

Session Commits (all on main, pushed):
- a48f71a: Mark phases 3-6 complete in ROADMAP + STATE
- 884cf4c: Remove obsolete .auto-claude directory
- 20a0bcc: Patch Dependabot vulns (Authlib 1.7.2, pytest 9.0.3)
- d48ca84: Fix HTMX click on multiple-result selection cards (url_for in f-string)
- d03cefa: Fix None crash in _render_unified_profile (or {} pattern)
- 95c99cd: Add ODBC Driver 18 to Dockerfile for Azure SQL warehouse
- 2ebf955: Fix app context in CLI mode + SKU catalog guard
- 7398359 + d0192ee: Fix async app context for employee profile refresh (wrap gather in app_context)
- Azure AD: Granted AuditLog.Read.All permission to Who-Dis (SandCastle) app registration
</completed_work>

<remaining_work>

Operational (not code — needs deploy/config):
1. Deploy latest main to SandCastle — Dockerfile rebuild needed for ODBC drivers
2. Fix Genesys OAuth DNS — change login.usw2 to login.usw2.pure.cloud in container config
3. Verify employee profile sync — run refresh after deploy, expect 415 records
4. Verify profile photos load after sync completes

Roadmap:
- Phase 7: Compliance Polish — `/gsd:discuss-phase 7`
</remaining_work>

<decisions_made>

- Employee profiles/photos come from Azure SQL (Keystone warehouse) via pyodbc, NOT Graph directly
- Added ODBC to Who-Dis image (matches Crystal Ball pattern) rather than external sync job
- Wrapped async gather in single app_context() rather than per-task context push
- AuditLog.Read.All granted via az CLI for Graph sign-in activity data
</decisions_made>

<blockers>
- Genesys OAuth DNS: container can't resolve login.usw2 — needs FQDN fix (prompt prepared)
- Employee profile sync: untested end-to-end after Dockerfile + app context fixes (needs deploy)
</blockers>

## Required Reading (in order)
1. `.planning/ROADMAP.md` — phases 1-6 complete, phase 7 next
2. `.planning/STATE.md` — current position and session continuity

## Infrastructure State
- App live at whodis.sandcastle.ttcu.com (SandCastle container)
- Azure AD app: Who-Dis (SandCastle) — appId 65d55deb-8e0b-42e4-ae33-2a0166e8f0a6
- Permissions: User.Read.All + AuditLog.Read.All (both admin-consented)
- Genesys: DNS broken in container (login.usw2 instead of login.usw2.pure.cloud)
- Employee profiles table: empty until first successful sync after deploy

<context>
Bug-fix session triggered by production issues after Phase 6 deploy. All code fixes are committed and pushed. The remaining work is operational (deploy, DNS config, verify sync). Once those clear, resume roadmap at Phase 7.
</context>

<next_action>
Start with: Deploy latest main to SandCastle (triggers Dockerfile rebuild with ODBC). Then run `python scripts/refresh_employee_profiles.py refresh` inside the container to verify the sync works. After that, `/gsd:discuss-phase 7` to begin Compliance Polish.
</next_action>
